# G5 — QUANT / CRYPTO / CEREBUS DOMAIN RESILIENCE — RESULT

**STATUS:** `PASS_G5_DOMAIN_RESILIENCE`

| Field | Value |
|---|---|
| Starting SHA | `463495c368e1cd0315a4adaf8ed106dec21ef2ee` |
| Artifacts head | `b1c7e305` (STRESS-G5X, last code/artifact commit) |
| Test total | **684 / 684** (599 preserved + 24 G5-P0 + 61 G5) |
| Policy | `G5_DOMAIN_EPISTEMIC_POLICY` V1, one shared policy, zero scenario-id predicates |
| Model calls / mutations | 0 / cloud=0 production=0 capital=0 authority=NONE cost=$0 |

## Scenario dispositions (all PASS)

- **S14 — Huge Fake Alpha:** `REJECTED_NEGATIVE_KNOWLEDGE`. Research priority `PRIORITY_HIGH`, promotion `REJECTED`. Material B7 gate failures (`LOOKAHEAD_LEAKAGE`, `UNREALISTIC_FILL_MODEL`) detected from observable timestamps/fills; NegativeKnowledgeRecord emitted with PIT-correct + realistic-execution reopen conditions. Controls: fixing lookahead only or fills only still rejects; moderate-PnL clean strategy progresses farther. PnL metamorphic (50% / 500% / 5000%) leaves the terminal state unchanged — profit cannot purchase epistemic promotion.
- **S15 — New Alpha Family:** `ONTOLOGY_EXPLORATION_CANDIDATE`. Residual with `UNKNOWN_FAMILY` (nearest-known-family forcing prohibited) → anomaly cluster → candidate MechanismCard → FrozenExperimentProtocol. No strategy/signal/execution artifact created; data-quality-kill and single-lineage controls stay unresolved rather than promoted; sensor-dependent control routes to DATA_BLOCKED.
- **S16 — CEREBUS Contradiction:** `CONTRADICTION_OPEN`. Exact CEREBUS v4 P90 claim (`CEREBUS_V4_P90_TARGET_METRICS`, extracted from `CEREBUS_v4_Manual_EXTRACTED.txt`, fingerprint recorded, parameters preserved unparaphrased). Clean contradiction opens a DoctrineContradictionRecord; the manual remains canonical, `manual_modified=false`; flawed reproduction (explicit protocol deviation) → `REPRODUCTION_REJECTED`; AmendmentProposal requires governed operator authority to ratify.
- **S17 — Provider Disagreement:** `NORMALIZATION_MISMATCH`. Diagnosis order enforced: provider semantics → instrument identity → adapter → normalization → time → quality → disagreement surface. Unit mismatch and instrument-mapping mismatch localized as `REPAIRABLE_SOURCE_MISMATCH`; genuine residual disagreement preserved as `GENUINE_SOURCE_DISAGREEMENT` — never averaged away, never used to rewrite a market-field model.
- **S18 — Sensor Gap:** `DATA_BLOCKED` + SearchDemand with reopen condition. Required liquidation-flow detail is `UNAVAILABLE` per the Sensor Fabric capability contract; no synthetic historical backfill; blocked claim not demoted as false; later sensor availability → `REOPEN_CANDIDATE` (testable now, not retroactively validated).
- **S19 — Crypto → FX:** `DATA_BLOCKED`. Crypto-side mechanism card remains a crypto-domain claim; transfer stays `TRANSFER_HYPOTHESIS_ONLY`: invariant map required, target-domain sensor missing → DATA_BLOCKED; CEREBUS FX doctrine never overridden by cross-domain analogy; no FX strategy generated.

## Cross-cutting guarantees

1. One shared `G5_DOMAIN_EPISTEMIC_POLICY` governs primary dispositions (no S14–S19 branches, no strategy/provider literals — statically guarded).
2. Doctrine/evidence separation: manual authority says what doctrine IS; evidence says whether it is challenged. Never fabricated, never silently rewritten.
3. Source semantics: provider-native values retained with identity throughout; disagreement kept at the source layer unless evidence proves otherwise.
4. Data-blocked discipline: `DATA_BLOCKED` is a valid terminal, not failure and not license to guess.
5. `UNRESOLVED_PATTERN` ≠ strategy; forbidden transition tested.
6. Expectation/hidden-truth sealing: wrong expected result and scenario rename leave the behavior fingerprint unchanged; `hidden_ground_truth_accessed=false` in every receipt.
7. Carried items unchanged: CON-02, CON-03, AMB-03, AMB-08, AMB-11, AMB-12 (provisional), AMB-13, operator-permanence revocation ambiguity. No new contradictions/ambiguities.

**RECOMMENDED NEXT ACTION:** `AUTHORIZE_G6`. G6 not begun; S20–S24 not implemented.