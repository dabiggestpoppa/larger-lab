"""Platform abstraction layer.

Each platform directory (xhs/, tiktok/, instagram/, youtube_shorts/, douyin/)
implements the PlatformAdapter interface defined in `base.py`.

See `docs/en/platforms/writing-an-adapter.md` for the contract.
"""
