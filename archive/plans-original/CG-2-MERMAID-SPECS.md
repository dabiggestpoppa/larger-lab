# CG-2 MERMAID VISUAL SPECIFICATIONS + ANCHOR DIRECTIVE (MAD 2026-05-28)

## Anchor Directive — READ THIS FIRST
This phase does NOT require AGI, consciousness, human intuition, omniscience, or full reality simulation.

**What this phase IS:** Operational situational awareness — recognizing the operational environment surrounding a task.

**What this phase is NOT:** Infinite reasoning, unlimited abstraction, philosophy, recursive expansion, god-model cognition.

**Core principle:** Reduce operational blindness. That's it.

**Keep world modeling:** operational, bounded, lightweight, execution-oriented, context-relevant — NOT philosophical or infinitely recursive.

## Master Graph
```mermaid
flowchart TB
    TASK[Task] --> CONTEXT[Operational Context]
    CONTEXT --> DOMAIN[Domain Environment]
    DOMAIN --> FIELD[Active Conditions]
    FIELD --> IMPLIED[Implied Requirements]
    IMPLIED --> RELATION[Relationship Mapping]
    RELATION --> PRIORITY[Priority Adjustment]
    PRIORITY --> WORLD[Micro World Model]
    WORLD --> PLAN[Execution Planning]
```

## Operational Context Flow
```mermaid
flowchart LR
    TASK[Task] --> ENVIRONMENT[Environment Detection]
    ENVIRONMENT --> RISKS[Risk Context]
    RISKS --> DEPENDENCIES[Dependencies]
    DEPENDENCIES --> CONSTRAINTS[Constraints]
    CONSTRAINTS --> EXECUTION[Execution Readiness]
```

## Implied Structure Inference
```mermaid
flowchart TD
    TASK[Task] --> IMPLIED[Implied Structure]
    IMPLIED --> VALIDATION[Validation Needs]
    IMPLIED --> SAFETY[Safety Requirements]
    IMPLIED --> MONITORING[Monitoring Requirements]
    IMPLIED --> ROLLBACK[Rollback Requirements]
    VALIDATION --> EXECUTION[Execution]
    SAFETY --> EXECUTION
    MONITORING --> EXECUTION
    ROLLBACK --> EXECUTION
```

## Micro World Model Example
```mermaid
flowchart TB
    MARKET[Market] --> STRATEGY[Strategy]
    BROKER[Broker] --> EXECUTION[Execution Engine]
    STRATEGY --> RISK[Risk Controls]
    RISK --> CAPITAL[Capital]
    EXECUTION --> CAPITAL
```

## Contextual Priority Shift
```mermaid
flowchart TD
    NORMAL[Normal Conditions] --> RISK[Risk Increase]
    RISK --> DRAWDOWN[Drawdown Pressure]
    DRAWDOWN --> SURVIVE[Continuity Priority]
    SURVIVE --> STABILIZE[Stability Priority]
    STABILIZE --> EXECUTE[Execution]
```

## Full Phase 2 Execution Sequence
```mermaid
sequenceDiagram
    participant USER as Operator
    participant OC2
    participant CONTEXT as Context Layer
    participant WORLD as World Model
    participant IMPLIED as Implied Structure
    participant PRIORITY as Priority Engine
    participant PLAN as Planner

    USER->>OC2: Task
    OC2->>CONTEXT: Detect Operational Context
    CONTEXT->>WORLD: Activate Environment Awareness
    WORLD->>IMPLIED: Infer Missing Structure
    IMPLIED->>PRIORITY: Adjust Priorities
    PRIORITY->>PLAN: Generate Context-Aware Plan
    PLAN-->>USER: Governed Operational Output
```

---
_CG-2 Visual Specs + Anchor Directive | 2026-05-28 | Bounded operational cognition. Not infinite reasoning._
