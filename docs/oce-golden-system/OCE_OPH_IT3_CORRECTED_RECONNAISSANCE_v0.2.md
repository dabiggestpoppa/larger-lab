# OCE × OPH × IT³ — Corrected Reconnaissance v0.2

**Date:** 2026-09-24
**Branch:** `agent/oce-institutional-stress-suite-build`
**Authenticated head at review:** `ef30cb4955c9b7c1e241fe6218c685e94698430a`
**Evidence lineage (corrected):**
- `ef30cb49` = ingestion-docs commit `docs(oce): ingest OPH x IT3 source packet alongside research handoff` — archives the DOCX and markdown transcription; it is **not** a G8 PASS and does not change the G8 gate state.
- `31c68da2b53e899cf64fed393be468f758760008` = `STRESS-G8ARCH7-R: re-archive at the derived tree 2cf1bb4b` — the published G8 evidence package head (reviewer PASS).
- `2cf1bb4b6bc39b2a372851a02ec50d96758a8788` = `STRESS-G8ARCH7: one resolver for red and green row evidence, one derivation` — the **tested G8 code tree** derived via `stress-suite/scenarios/g8_tested_tree.py:derived_tested_tree()` and recorded in `stress-suite/evidence/G8_RESULT.md` / `G8_TEST_RESULTS.xml` (`raw 23f97af88ff31c0e` / `canonical 6fa5005e1d26dd32`) and `G8_EVIDENCE_RECEIPT.json`. Do not cite `2cf1bb4b` as the tested SHA of future OPH research code; a new tested tree will be derived when authorized code changes land.
**Prior plan snapshot:** `f2a1c8ac` / `31c68da2` planning handoff; `ef30cb49` fast-forward 31c68da2..ef30cb49 verified `0 0` left-right, `git ls-remote == local HEAD`.
**Correction scope:** This review re-reads the live code at `ef30cb49` and fixes two false negatives. No A–H code is written, no G8 receipt is modified, no G9/G10 is started, no merge is performed. It supersedes the Block E/F inventory in `OCE_OPH_IT3_RESEARCH_SUBSTRATE_PLAN_v0.1.md` on those two blocks and extends the same reuse test to every block.

---

## 1. Re-read results — the owners that were missed

### Block E — TransferInvariantMap existed

* `stress-suite/engine/domain.py:1491` `class TransferInvariantMap` — 13 axes: `source_domain`, `target_domain`, `source_definition`, `target_candidate_definition`, `source_observables`, `target_observables`, `units_scales`, `state_semantics`, `market_structure_assumptions`, `mechanism_invariants`, `known_broken_assumptions`, `required_sensors`, `falsifiers`; `structurally_sound()` checks `not known_broken_assumptions and mechanism_invariants and source/target observables`; `from_fixture()` / `to_dict()` at lines 1510 / 1544.
* `stress-suite/engine/g5r.py:1406` `TRANSFER_MAP_AXES = (...)` enumerates those 13 axis names.
* `stress-suite/engine/g5r.py:1415` `class TransferMapValidationResult { map_sound, axis_status, missing_axes, broken_assumptions }`
* `stress-suite/engine/g5r.py:1447` `def validate_transfer_map(tmap: TransferInvariantMap) -> TransferMapValidationResult` — iterates `TRANSFER_MAP_AXES`, reports `axis_status[axis]=bool(value)`, collects `missing_axes` for every non-`known_broken_assumptions` blank axis, treats any `known_broken_assumptions` as invalidating soundness: `sound = not missing and not broken`.
* Downstream owners that bind it: `engine/domain.py:1556` `class DomainTransferHypothesis { transfer_map: TransferInvariantMap, frozen_target_protocol_ref, mechanism_ref, claim_ref }` and `engine/g5r.py:1627` `def resolve_frozen_target_protocol(hypothesis, protocols)` / `engine/g5r.py:1375` `def resolve_source_evidence_refs`.

Prior reconnaissance incorrectly reported no owned transfer-map. The owned map exists and is the only authority for S19-style transfer validation. A second `RealizationMapRegistry` must not be created.

Separate what is already proved from what OPH §6/§16 still asks for — see matrix row E.

### Block F — FreezeChronologyProof existed

* `stress-suite/engine/domain.py:717` `class FrozenExperimentProtocol` — fields `protocol_id`, `mechanism_ref`, `dataset_ref`, `time_range`, `features`, `metrics`, `falsification_criteria`, `holdout_ref`, `cost_execution_assumptions`, `promotion_criteria`, `target_domain`, `fingerprint`, `frozen_before_result_evidence` (human note only, line 731), `freeze_seq`, `freeze_epoch`, `frozen_by`, `authority_basis`, `freeze_evidence_refs`; methods `structured_freeze_present()` (line 758), `canonical_dict()` (763, excludes fingerprint, includes all structured freeze fields), `compute_fingerprint_canonical()` (780, `deterministic_hex("frozen_protocol", canonical_dict)`), `from_fixture()` (738, auto-computes `fingerprint`).
* `stress-suite/engine/g5r.py:1481` `class FreezeChronologyProof` — `protocol_ref`, `stored_fingerprint`, `recomputed_fingerprint`, `fingerprint_valid`, `freeze_seq`, `result_seq`, `chronology_ok`, `structured_freeze`, `evidence_refs_resolve`, `status`, `reason`; `FREEZE_CHRONOLOGY_STATUSES` seven values `FROZEN_BEFORE_RESULT_VERIFIED` .. `FREEZE_EVIDENCE_REFS_UNVERIFIED` at 1468.
* `stress-suite/engine/g5r.py:1510` `def verify_freeze_chronology(protocol, result_seq, registry=None) -> FreezeChronologyProof` — five-rung fail-closed ladder documented at 1527: stored fingerprint exists → recomputed == stored → structured witness exists → refs resolve (registry required when refs declared) → `freeze_seq < result_seq`. Any failure returns with the corresponding status and reason; success is `FROZEN_BEFORE_RESULT_VERIFIED`.
* Adjacent freeze custody: `stress-suite/engine/g6_governance.py:180` `class EvalContractSnapshot` deep-frozen `_FrozenMapping`/`_FrozenSequence`, `is_deeply_frozen()`, `criteria_fingerprint = deterministic_hex("eval_contract_snapshot", ...)`, `freeze()` / `mutate_criteria()` refusing; plus `FutureEvaluationContractCandidate` (ER02). `engine/g5r.py:1605` `class FrozenProtocolResolution` and `engine/g5r.py:1627` `resolve_frozen_target_protocol`.

Prior inventory omitted this cube. The live freeze already proves current-object consistency and historical chronology against a logical `freeze_seq`, and requires structured witness + registry-resolved evidence refs (TC-01 / ER-01). The gap for a prospective-prediction custody is narrower than "no freeze" — see matrix row F.

---

## 2. Corrected A–H ownership matrix (exact file:line)

Every row applies the same reuse test: *if a current owner already closes the obligation, reuse it and mark `EXISTING_CAPABILITY`; if it covers the axis checks but not OPH-specific preservation/custody predicates, `EXTEND` that owner; never create a parallel evidence registry, lifecycle, worker fabric, gate, or freeze mechanism.*

| §13 block | Obligation (packet §6–§8, §13 handoff) | Exact existing owner / API (at ef30cb49) | What it already proves (cite, not claim) | Concrete missing behavior needed for OPH×IT³ diagnostic | Reuse test → Decision | Entry-point test if subsequently authorized (no code now) |
|---|---|---|---|---|---:|---|
| **A. Epistemic Claim Ledger** | Typed claim registry with seven OPH classes: `finite_theorem`, `certified_computation`, `reconstruction_implication`, `open_realization_map`, `diagnostic`, `prospective_test`, `frozen_prediction`; machine-readable premises, inputs/transform hash, protected invariants, realization maps, kill band, eligible-data cutoff, status/receipt; dependency graph ; no analogy → identity without structure-preserving map (§6 `Claim = (id, type, domain, premises, inputs, transform, output, invariants, realization_maps, falsifier, receipt)`). | `stress-suite/engine/evidence.py:23` `EvidenceRecord` (kinds `OBSERVATION`/`DETERMINISTIC`/`AGENT_CLAIM`/`INDEPENDENT_CONFIRMATION`, `EVIDENCE_KINDS` line 18; fields `record_id`, `claim`, `provenance`, `status ACTIVE/DEMOTED/DORMANT/SUPERSEDED`, `source_lineage`, `resolution_class`, `allocator`, `subject` relevance key, `EvidenceRecord.make()`); `engine/registry.py:86` `EvidenceRegistry` (`from_records()`, `register()`, `has()`/`resolve()`, duplicate-conflict, `LineageSummary`); `engine/lifecycle.py:78` `KnowledgeRecord` + `LifecycleEdgeTable` / `TransitionRecord` (M4 lifecycle `OBSERVED→CANDIDATE→ACTIVE` etc., provenance never deleted, `forever_lineage_locked`); `engine/domain.py:809` `DoctrineClaimRecord` (exact machine-representable manual claim); `engine/evidence.py` `EvidenceChannelVector` 8 A-010 channels (no scalar authority; `operator_summary` non-authoritative). | Fail-closed evidence path accounting, duplicate/conflict detection, lineage diversity (`source_lineage`/`method_runtime`), lifecycle legality, A-010 channel separation (no scalar, no hidden authority). Proven by `tests/test_g2r_evidence_registry.py`, `test_lifecycle_machine.py`, `test_adjudicator.py`. | The seven OPH claim classes are not first-class `EvidenceRecord.kind` or `KnowledgeRecord` status values; no typed view that maps `Claim.type` → dependency/promise map, falsifier, receipt linkage, and promotion-ladder status (`OBSERVATION`→`CANDIDATE`→`REPRODUCED`→`ROBUST`→`CONFLUENT`→`BRIDGED`→`FROZEN`→`PROMOTED` / `REJECTED`) distinct from lifecycle. `resolution_class` / `Barriers` exist but not a `claim class ∈ {finite_theorem … frozen_prediction}` type discriminator with typed premise graph. | **EXTEND** existing registry/lifecycle (add typed *view* over `EvidenceRecord`/`KnowledgeRecord`; no second registry, no second promotion path, no new evidence channel). | Instantiate `EvidenceRegistry` with `OBSERVATION` source records + `KnowledgeRecord` transitions; publish a read-only `ClaimLedgerView` row for each `KnowledgeRecord` that asserts its `claim_class ∈ OPH7` and dependency closure; assert a pure function `claim_view(record) -> typed_row` without creating new storage authority; green: blocked transition rejects `frozen_prediction` without `freeze_proof == FROZEN_BEFORE_RESULT_VERIFIED`. |
| **B. Confluence Harness** | Repair/normalizer producing schedule-independent public normal forms: `x --σ1→ NF(x), x --σ2→ NF(x) ⇒ NF_{σ1}=NF_{σ2}`; termination, local diamond, completeness → public normal form idempotent and presentation-insensitive (§2, §5 table). §13 experiment 1 requires protected public vs nuisance split, valid-schedule enumeration within declared bound, termination/idempotence/local-diamond/final-state confluence, seeded order-sensitive control, minimized divergent trace. | `stress-suite/engine/replay.py:33` `ReplayEvent{seq, event_type, machine, actor, target, payload, contract_version}`; `engine/replay.py:54` `DeterministicReplay{phase, lifecycle, authority, clock, executor}` `run(events) -> ReplayResult{terminal_phase, terminal_lifecycle, trace, fingerprint}` strictly `seq`-ordered, `ReplayInputError` on out-of-order/duplicate `prev` check line 93, `deterministic_fp` over `phase.state + terminal_lifecycle + trace`; `engine/governed.py:58` `GovernedTransitionExecutor`; `engine/phase.py`/`engine/lifecycle.py` edge tables; `engine/base.py:68` `ReplayClock`; `engine/fixtures.py:53` `spec_to_replay_events`; `engine/g6_governance.py` deep-freeze for eval contracts. | One deterministic linearization per `StressScenarioSpec` with governed legality and stable fingerprint; out-of-order/duplicate rejection; reproducibility (`tests/test_replay.py`, `test_g1r_replay_contract.py`, `tests/test_g8_contradiction.py` red transcripts `g8_pre_repair_red_transcript.py`/`g8_arch_red_transcript.py` `HARNESSES` `PROBE_IDS`/`PRE_PASS_HEADS`/`verdicts`). | No harness that (i) declares a *protected-state projection* (which fields are public protected vs nuisance/presentation), (ii) enumerates the set of *valid* schedules up to stated bound (not arbitrary permutations violating prerequisites), (iii) asserts `termination` (eventually terminal phase) + single-action idempotence `NF(NF(x))=NF(x)` + `local-diamond` for independent steps + protected final-state equivalence across valid orders, with minimal counterexample trace on `CONFLUENCE_FAILURE`. Current replay expects one `seq`-linear trace; it does not generate valid-action permutations or compare protected hashes. | **EXTEND** `engine/replay.py` (add `schedule_enumerator(valid_actions_fn, bound) → valid_permutations` + `protected_projection(fingerprint)` + `confluence_verdict` yielding `PASS`/`CONFLUENCE_FAILURE{trace, minimal_prefix, divergent_hashes}`); no new worker fabric or canonical state store. | Take bounded `StressScenarioSpec` S01_WEAK (or S03–S04) with declared independent steps; enumerate ≤ N! valid permutations within bound; run each through `DeterministicReplay` with same seed records; assert `protected_hash` equality for pass case and minimal divergent pair for seeded order-sensitive control (expected `CONFLUENCE_FAILURE` traced to smallest reordered prefix). |
| **C. Invariant/Quotient Harness** | Quotient-visible protected invariants: define protected features, nuisance/presentation features, allowed transformations; verify quotient output invariant while presentation varies; refinement closure; §5 `Invariant engine: quotient-visible protected data` + `Perturbation invariance` / `Ablation` tests (§8 table). | `stress-suite/engine/g7_sensitivity.py:70` `PerturbationRecord{dimension, perturbation}`; `82` `BaselineBehaviorFingerprint{surface, subsystem, description, observables, digest}`; `101` `CounterexampleRecord{classification, baseline_observables, perturbed_observables, expected_relation, observed_relation}`; `122` `RelationVerdict{pass_, counterexample}` including `note`; metamorphic surfaces `M1–M17`, `con02`/`con03`, negative controls; harness compares `relation_expected` vs `relation_observed` on real engines vs pure surfaces, emits `CounterexampleRecord` with typed `classification` (already used by G8 controls). Plus `engine/registry.py` lineage, `engine/g8_contradiction.py` guarded properties. | Relation-preservation checks across 60+ metamorphic/sensitivity perturbations with deterministic surface comparison (`tests/test_g7_sensitivity.py` — renaming, duplication, file count, future-contract canonical content, allocator concentration, etc.). Detects when `available != adequate` or perturbation incorrectly changes rigor/immunity. | No declared `protected_projection / quotient` object: current `PerturbationRecord` carries `dimension`+`observables` but not an explicit `protected_fields ↔ nuisance_fields` partition, nor a `quotient = p(state)` function with `allowed_transforms` that must leave `quotient` stable. Packet requirement "quotient-visible protected data" is not an explicit first-class type in the engine. | **EXTEND** `engine/g7_sensitivity.py` (add `InvariantQuotientSpec{protected_fields, nuisance_fields, allowed_transforms, quotient_fn}` + `verify_quotient(projection, perturbations)` that reuses `PerturbationRecord`/`RelationVerdict`/`CounterexampleRecord` vocab). No parallel G7/G9 gate. | Declare `InvariantQuotientSpec` over existing scenario observables (e.g., phase/authority fields protected, file count or identifier spelling nuisance); apply `allowed_transforms = {rename_agent_ids, reorder_unordered_maps}` (M1/M5); assert `quotient_digest` unchanged for allowed transforms and `INVAR_VIOLATION` counterexample when nuisance transform leaks into protected field. |
| **D. State Reduction Arena** | Adversarial bench of ≥3 reducers on one input stream: continuous baseline, standard clustering (k-means/GMM), IT³-inspired deterministic quantizer, OPH-inspired repair-to-normal-form reducer; freeze metrics before comparison; score stability, compression, predictive/reconstruction fidelity, perturbation sensitivity, false structure on synthetic nulls; `OPERATOR_INDUCED` vs `CROSS_METHOD_ROBUST`; `INSUFFICIENT_DATA` when no stream has independent variation (§7 Blocs D, §13 Experiment 2, §8 tests). | Existing *inputs*: stress-suite scenario event/state streams (`S01_WEAK` 5-observation weak-contradiction stream, plus full S01–S24 scenario runners producing bounded `ReplayResult` fingerprints), and `engine/g7_sensitivity.py` null-generation patterns. No existing reducer framework — this obligation has **no current owner** beyond the input streams and the G7 null pattern library. | Real deterministic fixture streams, stable fingerprints, labeled weak vs full evidence scenarios. Warned in plan: `S01 5-observation stream is a candidate diagnostic, not adequate default for predictive advantage` — tautological fidelity if coarse labels are preserved. | Entire comparison harness: common `ReductionArena{source_stream_selection_rule, train/test split, preprocessing, null construction, metrics, seeds, parameters, stopping_criteria}` frozen pre-comparison; the three reducer implementations + continuous baseline; scoring functions (stability under perturbation, compression/DL, held-out reconstruction/prediction fidelity, false-positive rate on matched synthetic nulls); promotion gates `OPERATOR_INDUCED` vs `CROSS_METHOD_ROBUST`. | **EXTEND** with new harness `engine/oph_state_reduction.py` (or under `scenarios/`) that **plugs into existing streams** (no model/training pipeline, no invented data authority, no new dataset). If no stream meets `minimum independent variation` rule, emit `INSUFFICIENT_DATA` and leave comparison unpromoted. | Frozen experiment config hashes (metrics, split, preprocessing, null seed) before reducer runs; run 4 reducers on same held-out split of declared stream; publish `results_table { reducer, stability, compression, fidelity, null_fp }` with negative results; assert that a reducer whose discreteness is forced by operator (e.g., k=2 fixed) labels `OPERATOR_INDUCED` and cross-method-agreeing partition surviving nulls labels `CROSS_METHOD_ROBUST`, otherwise `REJECTED`. |
| **E. Realization Map Registry** | Every cross-layer mapping must declare what it preserves; reject "looks similar" automatically. Packet §5 `RealizationMap = (source_type, target_type, preserved_operations, preserved_order, preserved_normalization, refinement_rule)` and §6 requirement `preserved_operations / overlap / causal order / normalization / refinement` tested; classifications `ANALOGY_ONLY` vs `PERMITTED/FORCED/EXCLUDED` (§4). | `engine/domain.py:1491` `TransferInvariantMap` + `engine/g5r.py:1447` `validate_transfer_map()` (see §1 above: 13-axis structural completeness, `map_sound = not missing and not broken`); plus `engine/domain.py:1543` `DomainTransferHypothesis{transfer_map, frozen_target_protocol_ref, mechanism_ref, claim_ref}` and `engine/g5r.py:1627` `resolve_frozen_target_protocol()` verifying `target_domain` binding + `mechanism_ref` vs `hypothesis.mechanism_ref` equality and `fingerprint == recomputed canonical` + `structured_freeze_present()`. Validation currently covers **axis-populated completeness** only. | Per-axis `axis_status`/`missing_axes`/`broken_assumptions` and domain+mechanism binding checks are proved (`tests/test_g5r.py` transfer-map axes tests lines 1188–1224: empty units/state/market/falsifiers/required_sensors each invalidate). What it **already** validates: structural soundness — every mandatory axis non-empty and no `known_broken_assumptions`. What it **does not yet** validate: OPH realization obligations — preserved algebraic operations, overlap agreement `J_O(res d_P) = J_O(τ res d_Q)`, causal order preservation, normalization preservation, refinement-tower closure. The flag `analogy != transfer` is currently enforced only on name-collision / empty-axis level, not on operational/refinement preservation predicates. | Preservation predicates `preserved_operations`, `preserved_order`, `preserved_normalization`, `refinement_rule` as typed invariants on top of the 13 axes, with `bridge preservation tests` that fail a map with `ANALOGY_ONLY` (labels/dimensions match but operations/order/refinement do not). Carry §16 OPH caution: `preserved_operations / overlap / causal order / normalizations / refinement` otherwise `ANALOGY_ONLY`. | **EXTEND** `engine/domain.py:TransferInvariantMap`/`engine/g5r.py:validate_transfer_map()` (add `preserved_operations: Tuple[str], preserved_order: Tuple[str], preserved_normalization: Tuple[str], refinement_rule: str` fields and preservation-result witnesses; `validate_transfer_map` returns additional `preservation_ok` / `preservation_failures` distinguishing `ANALOGY_ONLY` vs `STRUCTURALLY_SOUND`; `provenance: unproven obligations → OPEN_BRIDGE`). **Do not propose a second registry/authority** — the corrected decision is to extend this owner. | Construct two maps equal on 13 structural axes except `preserved_operations=("commutator-closure",)`; map A proves its operation witness (e.g., overlap translation preserves `commutator` closure), map B declares `preserved_operations` but provides empty witness; `validate_transfer_map` asserts `map_sound=True` for A (preservation verified) and `ANALOGY_ONLY` + `open_bridge: ["preserved_operations"]` for B. |
| **F. Frozen Prediction Custody** | Hash hypothesis + code + params + metric + kill band + eligible-data cutoff before evaluation; append-only receipt; prospectively fixed conditional test (target, kill band, precision floor frozen before comparison); frozen prospective branch prediction — forward calc frozen before eligible data, failure rejects branch not entire framework (packet §2 ledger, §§12–14, §6 `Test = (hypothesis, baseline, perturbations, frozen_metric, kill_band, data_custody, result)`). Post-hoc diagnostics must report separately. | `engine/domain.py:717` `FrozenExperimentProtocol` (fingerprint `deterministic_hex("frozen_protocol", canonical_dict)` over 16 canonical fields, `frozen_before_result_evidence` note-only + `freeze_seq`/`freeze_epoch`/`frozen_by`/`authority_basis`/`freeze_evidence_refs`, `structured_freeze_present()`, `canonical_dict`/`compute_fingerprint_canonical()`); `engine/g5r.py:1481` `FreezeChronologyProof` + `1510` `verify_freeze_chronology()` (5-rung ledger, `FROZEN_BEFORE_RESULT_VERIFIED` only when `recomputed==stored && structured_freeze && refs resolve && freeze_seq<result_seq`); `engine/g5r.py:1605` `FrozenProtocolResolution` + `1627` `resolve_frozen_target_protocol()` (target_domain + mechanism-mediated binding + fingerprint recomputed check); `engine/g6_governance.py:180` `EvalContractSnapshot` deep-frozen `_FrozenMapping/_FrozenSequence`, `criteria_fingerprint`, ER01 structural nondistributivity. | Current-object consistency + historical chronology for *protocol freeze* (`freezing a metrics/protocol record before result seq`). Proven by `tests/test_g5rer.py` freeze-chronology proofs and `tests/test_evalcontract.py`. Distinguishes freeform text (`frozen_before_result_evidence` alone is `FREEZE_RECORD_INCOMPLETE`) from structured witness. What it **already** proves: a `FrozenExperimentProtocol` was frozen before an observed `result_seq` against registry-resolved evidence refs. What it **does not yet** custody: a **prospective prediction branch** — binding a prediction calculation hash + `eligible_data_cutoff` (no target-informed selection via cutoff leakage) + kill band/precision floor + code/config hash bifurcation (cf. §11 L6 `FROZEN`), with diagnostic-vs-prediction suppression (diagnostics that consumed measured values are labeled `DIAGNOSTIC`, never `PREDICTION`). | Attach a `FrozenPredictionCustody` record `{ prediction_ref, protocol_ref (FreezeChronologyProof status), source_refs, target_binding, hypothesis/mechanism binding, code_hash, metric, kill_band, eligible_data_cutoff, data_custody_hash }` to existing `FreezeChronologyProof` + `resolve_frozen_target_protocol` lineage, with `eligible_data_cutoff` proof (`data observed before cutoff` bit). No hash rewrite after cutoff; post-hoc diagnostics appended with separate `DIAGNOSTIC_RECEIPT` not `PROMOTION` claim. | **EXTEND** owners in `engine/domain.py`/`engine/g5r.py`/`engine/g6_governance.py` (add `FrozenPredictionCustody` type that **reuses** `verify_freeze_chronology` + `resolve_frozen_target_protocol` + `EvalContractSnapshot` deep-freeze; add `eligible_data_cutoff` + `kill_band` fields and `verify_prediction_custody()` that calls existing `verify_freeze_chronology`). **No competing freeze** — extend custody, keep retrospective "prediction" refusal. | Freeze protocol at `freeze_seq=10` with `freeze_evidence_refs` resolving; register prediction `code_hash="abc"` + `eligible_data_cutoff="2026-09-23"`; attempt evaluation at `result_seq=20` with data carrying `Provenance.observed_at="2026-09-24"` (post-cutoff) and `verify_freeze_chronology` → `FROZEN_BEFORE_DATA_VERIFIED` pass **only with the §7.2 provenance extension** — at `121bacd5` timestamps are absent, so without the extension any eligible-data case fails closed to `CUSTODY_UNVERIFIED`/`DIAGNOSTIC`; attempt with `observed_at="2026-09-22"` (pre-cutoff) or `provenance.*_at is None` → `CUSTODY_UNVERIFIED`/`RETROSPECTIVE` even though `freeze_seq < result_seq` holds; attempt at `result_seq=8` → `RESULT_PRECEDES_FREEZE` regardless of custody. Diagnostics that consumed measured values stay `DIAGNOSTIC` when `TARGET_CONTAMINATED` or timestamps unverified. |
| **G. Countermodel Generator** | AI as research adversary constructing alternate mechanisms reproducing same output with fewer/simpler premises; promotion requires surviving simpler explanations; compression/DL test (packet §8 agent roles `Red Team`/`Null Builder`/`Ablation Agent`, §7 G, packet §16 OPH caution `every claimed universal invariant must compete against null models`). | `engine/g7_sensitivity.py:101` `CounterexampleRecord{classification ∈ {TEST CONTRACT WRONG, IMPLEMENTATION BUG, POLICY AMBIGUITY, ARCHITECTURE CONTRADICTION, EXPECTED NON-INVARIANCE, UNKNOWN}, baseline_observables, perturbed_observables, expected_relation, observed_relation}` + `RelationVerdict`/`BaselineBehaviorFingerprint`; G8 counterexample register controls (`evidence/G8_COUNTEREXAMPLE_REGISTER.json` seeded inconsistency etc.); `engine/negative.py` `NegativeKnowledgeRecord` reopen conditions. | Minimal counterexamples with typed classification and preservation evidence; metamorphic null builders (duplicate/alias/randomized surfaces) partially cover "simpler explanation" idea. | Generator that *minimizes* a counterexample (smallest valid replayable trace producing divergent protected hash / invariant violation) and systematic `Null Builder` suite (random quantizers, k-means/GMM, trivial discretizers, graph community methods) competing against claimed invariant, plus `Ablation` single-premise removal measuring dependence — currently only ad-hoc perturbations, not a generative `CountermodelGenerator` with `compression_gain` / falsifier. | **EXTEND** `engine/g7_sensitivity.py` (`class CountermodelGenerator { generate_countermodels(claim) -> CounterexampleRecord[] , generate_nulls(invariant) -> BaselineBehaviorFingerprint[] , ablation(report) }` reusing existing `CounterexampleRecord`/`RelationVerdict` types). No parallel counterexample register or truth gate. | For a claimed universality invariant (e.g., "run operator labelled S_n partitions inputs"), invoke `CountermodelGenerator` to produce an `ANALOGY_ONLY` null model that reproduces same `S_n` histogram on fixture inputs via trivial quantizer; publish `CounterexampleRecord{ classification=ARCHITECTURE_CONTRADICTION, expected_relation="invariant holds under null", observed_relation="trivial quantizer matches invariant on this dataset" }` forcing `ROBUST` → `REJECTED` unless invariant survives perturbation + null suite. |
| **H. Closure / Self-Consistency Audit** | For subsystem inferring spec then executing it, compare inferred spec vs implementation vs observed public behavior; measure fixed-point residual rather than assuming `T(U)=U` closure (§2 closure fixed-point hypothesis, §5 H, §§15–16). | `stress-suite/scenarios/g8_closure_evidence.py` — single owner for RED+GREEN+artifact+survival `require() / problems() / resolve() / HARNESSES=(RED,ARCH) / TEST_MODULE / PROBE_IDS / PRE_PASS_HEADS / verdicts / annex derived not declared` (post-G8ARCH7 one derivation per row, byte-identical `G8_AUDIT_CLOSURE_MATRIX.md` `0b3e262468407d46`, refusals: bogus green name, bogus artifact, empty green_tests); `scenarios/g8_emit_evidence.py` delegates formatting to `require`; `_AUDIT_CLOSURE` 16 rows, `AUTHORITATIVE_TEST_COMMAND` vs `ARTIFACT_COMMAND` split per STRESS-G8ARCH7; `scenarios/g8_tested_tree.py:8` `derived_tested_tree()` sole tested-tree derivation; `engine/g8_test_evidence.py:508` `verify_citation(*, repo_root, expected_tested_sha)` keyword-only, `read_test_evidence`, `canonical_artifact_digest` `JUNIT_XML_MINUS_VOLATILE_ATTRS_V1`, `ARTIFACT_RELATIVE_PATH = stress-suite/evidence/G8_TEST_RESULTS.xml`; `engine/g8_contradiction.py` gate. | Cross-scenario contradiction blocks, gate-claim `BLOCKED`/`SUPERSEDED`, tested-tree derived-not-declared, citation `verified` only when artifact bytes hash to `raw+canonical` and name `derived_tested_sha`. `tests/test_g8_contradiction.py` 85+ closure resolver probes (once-derivation, dual-module `scenarios/g8_*` identity patch targeting `CLOSURE.ARCH` (the published matrix) not only the top-level module). | Composition that *reuses* closure evidence to compute a **closure residual**: `spec (claimed dependency graph) vs implementation (actual evidence registry + lifecycle) vs observed public behavior (replayed protected state)` → typed `ClosureAudit{ spec_hash, impl_hash, observed_state_hash, residual_metric, classification ∈ {CLOSED, DRIFTED, CONTRADICTS_SPEC} }`. Not a second G8 verdict; not a replacement of historical `G8_EVIDENCE_RECEIPT.json`. | **EXTEND** `scenarios/g8_closure_evidence.py` / `g8_run_audit.py` composition (compose declared claims + inputs + negative controls + tested-tree citations into `ClosureResidualReport`; surface unsupported promotions as `open_bridge`). No second verdict writer, no unreviewed historical-receipt rewrite. | Given a claimed architecture doc (e.g., OCE-CLAIM-closure), run existing `require()` + `verify_citation(repo_root, expected_tested_sha=derived_tested_tree())` + replay `deterministic_fp` of the cited workflow; produce `ClosureResidualReport` with `inferred_spec ≠ observed_norm_form` divergence when a spec field is consumed without evidence — assert `CONTRADICTS_SPEC` counterexample trace (minimal events where `NF_spec != NF_observed`). |

---

### Invariants for any later authorized implementation

* Never create a second `EvidenceRegistry` / lifecycle / worker fabric / freeze / authority to mirror packet nouns — the packet's import of authority is blocked by design.
* `EvidenceRecord.kind` stays FAIL-CLOSED four kinds; OPH seven become a **view**, not a new channel.
* `TransferInvariantMap` remains the single transfer authority; add preservation predicates there, not a parallel `RealizationMapRegistry`.
* `FreezeChronologyProof` / `FrozenExperimentProtocol.verify` remain the chronology truth; prediction custody wraps them.
* `DeterministicReplay` fingerprint is advisory provenance unless its protected projection is declared — do not promote `fingerprint equality` to semantic confluence without projection.
* `G8 prior_gate_receipts()` discovery is receipt-filename sensitive — new diagnostic artifacts MUST use a distinct namespace (e.g., `evidence/diagnostic/OPH_*` or `evidence/RESEARCH_*`) or amend auditor scope as an explicit reviewed contract change, never masquerade as `*RECEIPT*.json` at `evidence/` depth.
* Keep handling-up parity: `pytest tests` from repo root vs `python -m pytest tests -q` vs `scenarios/g8_*` top-level+package identity — patch must target `CLOSURE.ARCH` (the published matrix) not only the top-level module.

---

### Left-open debt still carried (unchanged from STRESS-G8ARCH7)

`declared()` helper unused; dual module identity patch shape (`CLOSURE.ARCH` vs top-level) remains; harness still duplicates ~50 lines of `PROBE_IDS`/`PRE_PASS_HEADS` indexing; row-table `G8_AUDIT_CLOSURE_MATRIX.md` is 320+ derived lines. These are non-blocking, recorded technical debt.

---

### Witness clarification (v0.3 patch — supersedes v0.2 paragraph; see decision packet v0.3 §7 for binding text)

**Block E correction to this matrix's E/F row wording:** A populated `preserved_operations` / `preserved_order` / `preserved_normalization` / `refinement_rule` is a declaration, not proof. The `EXTEND` for `TransferInvariantMap` + `validate_transfer_map()` therefore must add a **witness predicate**: each `preserved_*` declaration requires an independently resolvable witness edge (e.g., overlap-translation tau reconciling `res` lineages + trace digest match). Unresolved witness -> `ANALOGY_ONLY` + `OPEN_BRIDGE=[preserved field]` even when the string is populated; only witness-VERIFIED may gate `DOMAIN_VALIDATION_REQUIRED`. Entry-point test corrected: Map A witness resolves, Map B same strings but empty/non-resolving `evidence_refs`/`source_lineage` -> B stays `ANALOGY_ONLY`.

**Block F correction (v0.3):** `verify_freeze_chronology: freeze_seq < result_seq` proves chronology, not prospectiveness, and is the only freeze provenance that is implemented at `121bacd5`. `EvidenceRecord`/`EvidenceRegistry`/`Provenance` do **not** expose `observed_at`/`ingested_at` at that head (see `engine/base.py:164`, `engine/evidence.py:23`, `engine/registry.py:86`); do not describe registry timestamp resolution as existing capability. `FrozenPredictionCustody` that wraps `verify_freeze_chronology` must therefore independently verify **eligible-data custody** via the smallest provenance/data-custody extension specified in decision packet v0.3 §7.2 (`Provenance.observed_at/ingested_at` + registry propagation + `verify_prediction_custody` wrapping chronology + frozen `eligible_data_cutoff`/`kill_band` binding), with fail-closed `CUSTODY_UNVERIFIED`/`DIAGNOSTIC` (`RETROSPECTIVE` class) until that extension is implemented and evidenced. Keep `freeze_seq < result_seq` as the separate, already-implemented chronology check; custody is the independent prospective check. See decision packet v0.3 §7.2 for the three-case entry-point test (post-cutoff PASS only with extension, pre-cutoff/`None` timestamp → `CUSTODY_UNVERIFIED` even though chronology holds, result-before-freeze refusal) and quarantine to `evidence/diagnostic/`.
