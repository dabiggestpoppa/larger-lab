# OCE × OPH × IT³ — master agent handoff v0.1

Copy the following prompt to the implementing agent along with the user's *OCE OPH IT3 Cross Realization Ingestion Packet*. Read [the planning map](OCE_OPH_IT3_RESEARCH_SUBSTRATE_PLAN_v0.1.md) first. This prompt does not ratify architecture or authorize G9/G10.

---

You are working in `dabiggestpoppa/larger-lab` on the **existing** branch `agent/oce-institutional-stress-suite-build`. The OPH × IT³ packet is an external research specification; Section 13 is the requested research handoff. Implement the smallest genuinely missing **diagnostic** substrate, using current OCE owners. Preserve model-sparse doctrine, canonical state outside cognition, authority, rights/policy, and existing gates. Physical ontology is not an OCE axiom. Do not create a new branch, merge, forward-port to `oce-program-build`, or change the MF-B0–B4 build.

**First, establish authority and state.** Authentically fetch the current remote head; the planning snapshot was `31c68da2b53e899cf64fed393be468f758760008`, not a promise about today's head. Fast-forward only; preserve other agents' work and untracked files. Inspect `AGENTS.md` and ratified authorization rules. This prompt requests bounded research work but is not itself G9 authorization or an exception to constitutional increment gates. If the current rules require a separate exact stage authorization before *any* implementation here, stop after a reviewable implementation-ready plan and identify the exact missing authorization. Otherwise stay within a nonauthoritative research/test surface. Record baseline suite command, observed count, tool versions, head and clean/dirty status before edits.

**Reconnaissance before construction.** Read the packet, `docs/oce-golden-system/OCE_OPH_IT3_RESEARCH_SUBSTRATE_PLAN_v0.1.md`, current OPH/Cadence impact review and A012, G8 result/closure, and current stress-suite engine/scenarios. Build a table for A–H with: obligation; exact existing owner/API; what it already proves; concrete missing behavior; decision `REUSE / EXTEND / NO BUILD`; entry-point test. In particular inspect `EvidenceRegistry`/`EvidenceRecord`/`KnowledgeRecord`, replay, G7 sensitivity, `TransferInvariantMap`/`validate_transfer_map`, G6/G5R freeze custody, and G8 closure/tested-tree. Never create a second authority, evidence registry, lifecycle, worker system, gate, or freeze mechanism merely to mirror the packet's nouns. Show this table before making a block's change.

**Freeze the experimental contract first.** Type claim statuses separately: theorem; computation; diagnostic; conditional implication; open realization map; custody-frozen prediction. Record premises, input and code/config hashes, source vs target dependence, falsification condition and precise owner. An analogy can become identity only through a tested structure-preserving realization map, including operations, overlap, causal order, normalization and refinement. Changes to the contract that affect a verdict must preserve pre-change results and explain the revision.

**Build in dependency order, only for missing behavior:** A typed claim-status view on current evidence, B bounded schedule-confluence tests on current replay, C explicit protected-state/quotient and invariant obligations on existing sensitivity controls, D common-input four-method reduction arena, E preservation checks on the current transfer-map owner, F existing freeze chronology extended to predictions only when actually pre-data, G minimal counterexamples/null generators on current counterexample machinery, H composition with current closure audit. `EXISTING_CAPABILITY` plus evidence is an acceptable outcome for any block. Keep research outputs nonauthoritative; do not claim a G8 or G9 PASS from them.

**Experiment 1.** Choose one existing deterministic workflow using declared eligibility criteria and no external effect. Fix protected public state vs nuisance fields before seeing verdicts. Enumerate valid schedules within a stated bound and test termination, idempotence where meaningful, local-diamond behavior and protected final-state confluence. Seed at least one order-sensitive negative control. Minimize every failure to its smallest valid replayable trace. Say `INCONCLUSIVE` beyond tested bounds.

**Experiment 2.** Select an existing event/state stream using a declared rule; don't elevate the five-observation S01 stream to predictive proof if its coarse labels make fidelity tautological. If no suitable stream exists, record `INSUFFICIENT_DATA` instead of inventing a result. Freeze metrics, held-out split, preprocessing, seeds, reducer parameters and null construction before comparison. Run a continuous baseline, ordinary clustering, IT³-inspired deterministic reducer and OPH-inspired repair-to-normal-form reducer on the same inputs. Report stability, compression, held-out reconstruction/predictive fidelity, perturbation and false structure on matched synthetic nulls. Target-informed constants are diagnostic. Forced discreteness is `OPERATOR_INDUCED`; cross-method agreement needs independent methods plus null/perturbation survival. Negative outcomes belong in the table.

**Protect the existing evidence chain.** G8 discovers receipt-like filenames; put new diagnostic artifacts in a transparently indexed distinct namespace or amend auditor scope under review, never quietly masquerade as a historical gate receipt. Verify `g8_tested_tree` and JUnit citation rules before changing code/tests. Run the authoritative suite and the artifact-producing command from the tested tree; publish each command's actual pass/skip count separately. Re-emit/archive only artifacts causally changed; test deterministic replay and citation hashes. Do not add package markers or change import identities without proving both invocation paths. No speculative rewrites, mass formatting or cosmetic Sonar cleanup.

**Exit report and delivery.** Produce an architecture note, minimal schemas or typed views, entry-point tests, diagnostic receipts, counterexamples, dependency graph, claim-status ledger, comparison table, explicit `NOT_IMPLEMENTED` inventory, unchanged boundaries, counterfactual controls, exact commits and remote head. Show red-before/green-after for any repaired defect, not for harmless additions. If a claim fails, keep the failure visible. Run `git diff --check`; verify only intended paths changed; push a fast-forward to the existing branch only when within applicable authority and tests are green. Do not merge. Provide a next prompt only after the authoritative suite passes. If authority, evidence custody, or dataset sufficiency blocks the work, report that precise blocker and the smallest decision needed; do not manufacture a PASS.

---
