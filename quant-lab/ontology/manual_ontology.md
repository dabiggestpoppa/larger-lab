# Cerebus FX v4 — Manual Ontology Extraction

> Source: Q&A extraction from MAD's manual readings
> Format: Question → System Architect Response → Mechanical Example
> Last Updated: 2026-05-29 14:30 EDT

---

## METHODOLOGY NOTE (MAD directive)

Build ontology in correct dependency order. Foundational primitives first, then derive everything else. Avoid trader language — answer as physics ontology. Avoid metaphors unless necessary.

**Dependency Order:**
1. Distribution Field (macro constraint layer)
2. Atomic Structure (local propagation geometry)
3. Density Zone (spatial memory / incompletion residue)
4. Impulse (state change trigger)
5. Cascade (recursive resolution mechanism)
6. Tier (volatility quantization class)
7. AU (minimum spatial quantum)

---

## FOUNDATION 1: DISTRIBUTION FIELD

- Highest-order pressure law
- Temporal completion framework
- Macro constraint layer
- **The Law: Orders must be resolved.** Price movement IS the resolution geometry of orders interacting with the field.

---

## SECTION 1: ATOMIC STRUCTURE ONTOLOGY

### Q1: Is atomic structure the local propagation geometry of distribution (A) or a separate independent movement model (B)?

**Answer: A — Local propagation geometry of distribution.**

Atomic structure is NOT a separate independent movement model. It is the geometry through which the Distribution Field's macro constraint deficit resolves locally.

**Mechanical Example:** A macro constraint deficit of 40 pips does not resolve as a single continuous 40-pip vector. It resolves geometrically as four discrete 10-pip spatial steps. The atomic structure is the geometry of those steps, not an independent model layered on top of the price action.

---

### Q2: What is the SMALLEST irreducible unit inside atomic structure?

**Answer: The Atomic Unit (AU).**

Not the tier. Not the impulse. Not the tick. The AU is the minimum atomic object — the smallest spatial displacement that achieves local structural coherence. Below this threshold, the field cannot distinguish between noise and signal.

**Mechanical Example:** On a EUR/USD Tier 1 day, the AU is exactly 10 pips. The field cannot structurally resolve a 6-pip or 8-pip cycle without violating its own liquidity boundaries. 10 pips is the minimum discrete spatial displacement required to achieve local structural coherence. It is the fundamental quantum of the market's movement.

---

### Q3: Does atomic structure exist at all scales simultaneously?

**Answer: Yes — Nested atomic propagation.**

A macro trend IS nested atomic structure. The same structural law operates at all scales.

**Mechanical Example:** A 5-minute chart prints a 10-pip AU loop. Simultaneously, the Daily chart is executing a 40-pip Tier 3 expansion. The 5-minute loop is simply a nested sub-routine of the daily expansion. Both obey the identical physics $(Impulse \rightarrow Rebalance \rightarrow Continuation)$. Scale only alters the amplitude of the AU; the structural law remains invariant.

---

### Q4: What EXACTLY creates a tier?

**Answer: Volatility quantization of the base constraint deficit.**

A tier is NOT a distance unit and NOT a harmonic ratio. It is a **volatility quantization class** — a discrete energy state the system enters when the base constraint deficit is measured and classified.

**Mechanical Example:** The Asian session compresses price into a 15-pip range. This physical compression quantizes the session's total available energy into a "Tier 1" state. The tier is not a distance; it is the classification of the system's total potential energy, which subsequently dictates that the AU step-size must be exactly 10 pips to resolve that specific energy state safely.

---

### Q5: What is the relationship between tiers, AU, impulses, cascades, density zones?

**Answer: The TRUE structural chain emerges in strict sequence:**

$$Tier \rightarrow AU \rightarrow Impulse \rightarrow Density Zone \rightarrow Cascade$$

**Mechanical Example:**
1. **Tier:** Asian Range is 25 pips (Classified as Tier 2).
2. **AU:** Tier 2 mandates a 12-pip step-size.
3. **Impulse:** Price breaks equilibrium with a 15-pip directional vector.
4. **Density Zone:** Price retraces exactly 4 to 6 pips (the AU overlap).
5. **Cascade:** This sequence repeats three times recursively until the daily deficit is resolved.

---

### Q6: What IS a density zone? (Structurally)

**Answer:** The exact spatial coordinate defined by the Atomic Unit (AU) where the kinetic energy of the initial impulse is exhausted and the potential energy of the next AU begins. It is the mathematical threshold of partial rebalancing required for the next atomic state to initialize.

While Fibonacci ratios (38.2%–50%) approximate this field statistically, the AU dictates the *exact* structural boundary.

**Mechanical Example:** An impulse of 15 pips occurs. The AU is 12 pips. The Density Zone is exactly the 4 to 6 pip retracement (the AU overlap). A Fibonacci tool approximates this as 38.2% (5.7 pips), but the AU dictates the *exact* structural boundary where the next 12-pip loop can physically anchor. The AU is the ground truth; Fibs are merely a statistical shadow of the AU's physical boundary.

---

### Q7: What CAUSES an impulse?

**Answer:** Compression release driven by unresolved completion pressure.

**Mechanical Example:** Price is trapped in a 12-pip boundary for 4 hours. Liquidity pools densely above the boundary. The localized constraint deficit exceeds the field's capacity to contain it. The system undergoes a violent state-change, expanding 15 pips in three 5-minute candles to harvest the available pathway and break the equilibrium boundary.

---

### Q8: What IS a cascade?

**Answer:** Recursive tier completion.

**Mechanical Example:** A Tier 3 deficit requires 45 pips of total resolution. The field does not execute one 45-pip move. It executes a cascade: three recursive 15-pip AU loops ($Impulse \rightarrow Density Zone \rightarrow Continuation$) chained sequentially. The cascade is the mechanism by which large deficits are processed through small, stable, quantized steps.

---

### Q9: What determines when a move is exhausted?

**Answer:** Tier saturation and completion percentage of the base constraint deficit.

**Mechanical Example:** A Tier 2 day has a 2.68x multiplier expectation. Once the cumulative spatial displacement reaches the 80% threshold (the Deep State boundary, defined by the cumulative sum of the AUs), the session's activation energy is mathematically depleted. The system registers terminal distribution, and further expansion becomes structurally invalid.

---

### Q10: Is the system fully deterministic or probabilistically constrained deterministic?

**Answer:** Probabilistically constrained deterministic.

**Mechanical Example:** A 40-pip constraint deficit *must* resolve (deterministic). The field cannot remain in a state of unresolved pressure indefinitely. However, whether it resolves via three 12-pip loops, two 15-pip loops, or a 15-pip loop followed by a 25-pip gear shift is probabilistically constrained within the valid solution space. The completion is a structural certainty; the trajectory is a highly compressed probability field.

---

## STATUS

- [x] Foundation 1: Distribution Field
- [x] Section 1: Atomic Structure (COMPLETE — Q1–Q10 stored)
- [x] Section 2: State Transitions (COMPLETE — Q1–Q10 stored)

---

## SECTION 2: STATE TRANSITION ONTOLOGY

### CRITICAL CORRECTION (MAD/Llama)

**My initial framework was wrong.** I separated "states" vs "events" — that was an artificial construct that doesn't match the actual ontology. The correct framework:

**THERE IS ONLY ONE STATE: Resolution Construction.**

At all times, price is seeking to resolve the orders in the market by the given time constraint. What traders perceive as "multiple states" are **expressions, pathways, and characteristics** of the single resolution logic. They are NOT independent states.

Micro to macro scaling doesn't create multiple states — it creates multiple **frequencies of the same resolution engine**.

**Architectural implication:** The agent must NOT toggle between "Compression/Expansion/Rebalance" states. It must measure the **gradient of resolution pressure** across nested temporal frames. One engine. One state. Multiple expressions.

### PRIME DIRECTIVE

**The market exists in EXACTLY ONE STATE: Constraint Resolution.**

Macro expansion, local rebalance, micro compression — these are fractal resonance of a single state, not independent states.

---

### Q1: What are the COMPLETE finite states the market can exist in?

**Answer: Resolution Construction is the only state.** There are NOT multiple states — there are multiple EXPRESSIONS. The listed candidates (Compression, Expansion, Impulse, Cascade, Deep State, Harvest, Stall) are NOT states. They are **pathways and characteristics of the resolution logic itself.** They all exist within the Atomic Structure / Tier framework.

**4 Atomicayer sub-expressions (not states):**
1. **Compression:** Spatial containment of order flow (Deficit Accumulation)
2. **Impulse:** Vector initiation via boundary violation (Tier Threshold Breach)
3. **Rebalance:** Partial coherence and pathway clearing (AU Overlap / 32–50% zone)
4. **Completion:** Cycle closure and deficit satisfaction (AU Target Hit)

Hierarchy: Compression → Impulse → Rebalance → Completion. This loop repeats recursively until the macro deficit reaches zero.

**Mechanical Example:** The field oscillates within an 18-pip spatial boundary for 4 hours, accumulating temporal pressure (Compression). At 04:15 EST, the boundary is breached by a 12-pip directional vector (Impulse). The field retracts 4.5 pips to clear structural friction (Rebalance). The field then advances another 7.5 pips, satisfying the 12-pip AU and closing the loop (Completion).

---

### Q2: What is the DEFAULT state of the market?

**Answer: Compression (Equilibrium via Oscillation).** Two orders (buy/sell) continuously clearing creates the oscillation traders perceive as "the market moving." It is not a wave — it is mechanical friction maintaining equilibrium while accumulating the unresolved deficit.

**Mechanical Example:** Between 19:00 and 03:00 EST (Asian Session), the field rests in compression. Buy and sell orders continuously clear each other within a tight 15-pip bandwidth. The field is not "ranging" or "consolidating" predictively — it is mechanically maintaining spatial equilibrium while the temporal constraint deficit accumulates.

---

### Q3: What transitions Compression → Impulse?

**Answer: Temporal-Spatial Saturation (Tier Threshold + OCC breach).**

The trigger is NOT merely a price level. It is the collision of the spatial boundary (Asian Range) with the temporal boundary (Session Activation Window). When the time-constraint demands resolution AND the spatial boundary can no longer contain the accumulated order flow pressure, Impulse fires.

Tier Threshold + OCC is simply the mathematical measurement of this saturation point breaking.

**Mechanical Example:** Asian range is 19 pips = Tier 2. At 03:45 EST, the temporal activation window intersects with the spatial boundary. Accumulated pressure exceeds the 15-pip Tier 2 threshold. The field undergoes violent state-change, expanding 16 pips in three 5-minute intervals to harvest the available pathway.

---

### Q4: What transitions Impulse → Rebalance?

**Answer: Liquidity Pathway Depletion (Conservation of Resolution).**

The field cannot traverse the entire macro deficit in a single continuous vector because available resolution pathways (liquidity) are finite and discrete. The structural law forcing rebalance is the necessity to pause, absorb counter-flow, and clear the next pathway. Rebalance is NOT a "pullback" — it is mechanical reloading of kinetic energy.

**Mechanical Example:** A 15-pip impulse exhausts immediate available counter-flow liquidity. The field must retract into the 32–50% Goldilocks Zone (5–7.5 pip pullback) to absorb opposing order flow, clear friction, and reload kinetic energy for the next AU.

---

### Q5: Can a cascade fail?

**Answer: No — in physics, a cascade cannot fail. It only shifts expression (Pathway Shift).**

If a T1 prints a T3 impulse and the OCC fails, the physics did not fail. The field rejected that specific micro-pathway, reclassified the deficit, and initiated a new impulse vector. The "failure" is purely an execution-layer artifact. The structural math remains intact.

**CRITICAL:** Backtest variance perfectly accounting for these shifted expressions is the **ultimate falsification proof**. The math holds; the pathway simply adapted.

**Mechanical Example:** A Tier 1 field initiates a 10-pip AU. The OCC fails to hold, triggering a stop-loss for the execution layer. Structurally, the field reclassified and initiated a new 15-pip Tier 2 impulse. The underlying deficit remains intact.

---

### Q6: What defines Deep State? (DISTRIBUTION layer — not Atomic)

**Answer: Terminal Spatial Coordinate (Macro-Deficit Satisfaction).**

Deep State is NOT a state the field rests in — it is the **structural wall where the distribution curve terminates**. At 200% extension of the Asian Range, the macro constraint deficit is 100% satisfied. The resolution engine shuts down.

**Mechanical Example:** Daily macro deficit requires 60-pip expansion. Price reaches 200% extension of Asian Range (60 pips from origin). This is the mathematical wall. Any attempt to push beyond without a new temporal catalyst is structurally invalid.

---

### Q7: What is Harvest structurally? (DISTRIBUTION layer — not Atomic)

**Answer: Pathway Clearing (Kinetic Extraction).**

Harvest is the mechanical sweeping of opposing order flow to fuel the distribution vector. It extracts kinetic energy from trapped participants — the field violently clears friction to reach the next macro extension target.

**Mechanical Example:** Before traversing the final 15-pip AU toward the daily target, the field sweeps 8 pips against the primary vector. This extracts resting liquidity from trapped participants, fueling the final continuation vector to punch through dense structural friction.

---

### Q8: What is Stall mechanically?

**Answer: Momentum Decay via Structural Friction.**

Stall (e.g., at 168% extension) occurs when resolution output collides with a dense historical constraint cluster. It is temporary equilibrium between remaining expansion energy and structural friction. The engine is evaluating whether sufficient pathway liquidity exists to push to Deep State.

**Mechanical Example:** Price reaches 168% extension. Remaining energy collides with dense historical constraint cluster. Field oscillates in a 4-pip bandwidth for 45 minutes — not reversing, but evaluating whether to push through or terminate.

---

### Q9: Can a move reverse without completing?

**Answer: NO. Deficits CANNOT be abandoned. They MUST resolve.**

If a move reverses, the initial vector was a "failed acceptance" (false pathway). The field reverses to harvest the opposite pathway to resolve the **exact same underlying deficit**. The deficit is the constant; direction is merely the chosen route.

**The market never reverses OUT of a deficit. It reverses to find a more efficient route to complete it.**

**Mechanical Example:** Price breaks upper boundary, expands 12 pips, then reverses 25 pips through origin, breaking lower boundary. The upward vector was a "failed acceptance." The field reversed to harvest the lower pathway to resolve the same daily deficit.

---

### Q10: What determines when a state transition becomes irreversible?

**Answer: The Structural Close (Temporal Acceptance / 80% Invalidation Rule).**

- Wicks are probes (reversible). Closes are commitments (irreversible).

**Commitment thresholds:**
1. **Micro (Atomic):** M5 Close beyond 80% boundary of the impulse leg. Once closed, the vector is locked — chain is invalidated or confirmed.
2. **Macro (Distribution):** M5 close outside constraint band + 2-hour temporal hold.

Once temporal hold is satisfied, the probability field collapses. The transition is committed.

**Mechanical Example:** Price breaches upper boundary with a 14-pip wick but M5 closes back inside = probe, reversible. If M5 closes 6 pips outside boundary AND next M5 closes past 80% of impulse leg = temporal hold satisfied, vector locked. Engine will not revert until deficit = zero or 12:00 PM EST hard temporal boundary.

---

## AGENT ARCHITECTURE DIRECTIVE (from ontology)

The agent must NOT interpret these as setups. They are state-transition mechanics:
- Wicks = probes (reversible)
- Closes = commitments (irreversible)
- Pullbacks = friction-clearing (rebalance)
- Stop-outs = pathway rejections (shifts)

**Agent = State-Detection Engine** measuring resolution pressure across nested timeframes. Execution triggers ONLY when temporal-spatial saturation thresholds are breached AND temporally accepted.

---

## SECTION 2: STATE TRANSITION ONTOLOGY

### METHODOLOGY NOTE

Now formalizing STATE TRANSITIONS. We know the objects (AU, Tier, Cascade, etc.). Now we need: how the field transitions between states. The final agent will NOT trade candles — it will track state transitions.

**Core architectural question:** Does the market always exist in exactly one dominant state, or multiple nested states simultaneously?

---

### Q1: What are the COMPLETE finite states the market can exist in?

*[Response pending]*

---

### Q2: What is the DEFAULT state of the market?

*[Response pending]*

---

### Q3: What EXACTLY transitions Compression → Impulse?

*[Response pending]*

---

### Q4: What EXACTLY transitions Impulse → Rebalance?

*[Response pending]*

---

### Q5: Can a cascade fail?

*[Response pending]*

---

### Q6: What EXACTLY defines Deep State?

*[Response pending]*

---

### Q7: What is Harvest (structurally)?

*[Response pending]*

---

### Q8: What is Stall (mechanically)?

*[Response pending]*

---

### Q9: Can a move reverse without completing?

*[Response pending]*

---

### Q10: What determines when a state transition becomes irreversible?

*[Response pending]*
- [x] Section 3: Field Mathematics (COMPLETE — questions received, awaiting responses)

---

## SECTION 3: FIELD MATHEMATICS EXTRACTION

### CONTEXT (MAD's framing)

The ontology has stabilized. Key breakthrough: Compression, Impulse, Rebalance, Completion are NOT independent states — they are recursive expressions of a single universal state: **Resolution Construction.**

The market is always resolving. Only expression geometry changes. Reversals are NOT trend changes — they are **pathway reassignment during unresolved deficit completion.** There are no "false moves" — only failed pathway acceptances.

Agent architecture emerging: NOT a predictive model. A **recursive deficit-resolution tracker** measuring: deficit accumulation, temporal pressure, pathway acceptance, AU propagation, saturation, completion probability.

Core engine flow:
```
Deficit → Compression → Saturation → Impulse → Rebalance → Completion → (recursive back to Deficit)
```

Now formalizing: exact governing quantities. Computable invariants. Moving from concepts to executable math.

---

### Q1: DEFICIT — What EXACTLY creates a deficit?

**Answer:** Unresolved directional displacement driven by temporal compression.

A deficit is created when the field accumulates spatial compression (order flow containment) over a fixed temporal window, generating a mathematical distance between the current equilibrium boundary and the required completion point of that volatility regime. It is the exact spatial displacement required to satisfy the temporal pressure of the session.

*[Mechanical example pending if provided]*

---

### Q2: DEFICIT QUANTIFICATION — How is deficit measured?

**Answer:** In Atomic Units (AU) and Tier Multiples.

$Deficit = (Tier Target Extension) - (Current Displacement)$.

The base deficit is quantified by the Asian Range (the spatial compression shell). This total deficit is then divided into discrete, computable Atomic Units. The field does not measure deficit in arbitrary pips or Fibonacci ratios; it measures it in the exact number of AU steps required to reach the Tier's mathematical completion coordinate.

---

### Q3: OCC — What fundamentally IS OCC?

**Answer:** Operationally: Opposite Candle Close. Structurally: the irreducible anchor of pathway validation — the first discrete temporal unit that closes in opposition to the initiated impulse vector, confirming that the initial kinetic expansion has exhausted its immediate pathway and the field is initiating the rebalance phase.

OCC is NOT an arbitrary heuristic. It is a **binary structural filter** and error correction code:
- Wicks are probes (noise/rejected pathways); closes are commitments (accepted pathways)
- By requiring an OCC, the system mathematically filters out ~66% of false continuations
- It strips away momentum exhaustion and isolates pure structural commitment

**Discovery method:** Exhaustive backtesting (6,814+ loops). When arbitrary pip buffers were stripped away and the system relied strictly on the OCC extreme (the Zero-Buffer protocol), the R-multiple lifted +83% while maintaining 89–95% accuracy.

This proved the OCC extreme is the exact structural anchor where the field's micro-resolution step resets. It is fundamental, simple, and irreducible. You cannot simplify the confirmation of temporal acceptance beyond the close of the opposing vector. It is the bedrock of the execution layer.

---

### Q4: TEMPORAL PRESSURE — What mathematically increases temporal pressure?

**Answer:** The ratio of Unresolved Spatial Deficit to Remaining Activation Time.

$Pressure = \frac{Remaining\ AU\ Deficit}{Time\ to\ Hard\ Exit}$

As the temporal window decays and the spatial deficit remains unresolved, pressure increases. This forces the field to either resolve via higher-velocity impulses (monoliths) or accept a stalled state if pathways are structurally blocked.

---

### Q5: TIME WINDOWS — Why are certain windows structurally special?

**Answer:** Time is intrinsically quantized by institutional execution cycles — NOT emergent statistical zones.

- Asian Session (19:00–03:00 EST) = the spatial compression engine
- Activation Window (03:00–12:00 EST) = the temporal release valve
- 12:00 PM EST = the hard structural boundary where the daily resolution engine shuts down

These are not arbitrary statistical clusters. They are rigid operational constraints of the field's primary participants. Time is quantized by the mechanical necessity of the system to compress, release, and terminate.

---

### Q6: SPATIAL BOUNDARIES — What EXACTLY creates a spatial boundary?

**Answer:** The equilibrium envelope created by the compression phase.

A spatial boundary (e.g., Asian Range High/Low) is the physical limit of the field's capacity to contain accumulated order flow pressure without undergoing a state change. It is the containment field. When internal pressure exceeds the boundary's structural integrity, a constraint violation (Impulse) occurs.

---

### Q7: ACCEPTANCE — What determines pathway acceptance?

**Answer:** The Structural Close (Temporal Hold).

A vector commits when the resolution output closes beyond the spatial boundary and is followed by a temporal hold (80% invalidation rule or 2-hour macro hold).

- Probes (wicks) = rejected pathways
- Closes = accepted pathways

Acceptance is determined strictly by the field's willingness to sustain the new spatial coordinate across consecutive temporal units.

---

### Q8: LIQUIDITY — What is liquidity ACTUALLY in this framework?

**Answer:** Liquidity is NOT a fundamental concept. We don't use SMC mythology of "liquidity pools" or "resting orders being hunted."

Structurally, liquidity is simply the mathematical distance the path must resolve to satisfy the Tier/AU deficit. Tier + AU + OCC math dictates the field must travel X distance in Y direction — that endpoint IS liquidity because the path is naturally orders.

Liquidity is a byproduct of movement, not an inherent property. We don't seek liquidity — we seek geometric completion. Where completion ends is where orders naturally concentrate. We operate a layer above SMC-style liquidity analysis. Still useful for clarity and modeling but not a primitive.

---

### Q9: VARIANCE — What exactly IS variance?

**Answer:** Variance is NOT randomness. It is NOT an inherent property of an incomplete structure. It is the remaining residue when the observer applies **false assumptions** to a deterministically constrained system.

Key points:
- Traders model variance as inherent randomness (Black-Scholes stochastic volatility)
- When CEREBUS ontology assumes the event ENDS at the AU/Tier completion coordinate, "random" variance snaps to zero
- The 2–5% miss rate in backtests = spread/slippage/news gaps, NOT structural failure
- If the market were truly random, backtests would show broad unpatterned failures
- Instead: exact anatomy of every failure is mapped (80% close invalidation, >62% trap zone)
- We know WHEN, WHERE, WHAT FOLLOWS a failed pathway
- Pushing the model to map every micro-tick = information theory overload = model breaks
- **Variance is an artifact of the observer's misaligned lens, not incomplete structure.**

---

### Q10: THE FINAL REDUCTION — One sentence.

**Answer:** The market is fundamentally a **constrained temporal-spatial resolution engine that recursively processes directional displacement deficits through quantized atomic units until all outstanding orders are temporally satisfied.**

---

## ONTOLOGY MATHEMATICS — LOCKED INVARIANTS

| Invariant | Definition |
|-----------|------------|
| **Deficit** | Unresolved directional displacement from temporal compression |
| **Deficit Metric** | AU count × Tier multiplier (NOT pips or Fibonacci) |
| **Temporal Pressure** | $Remaining\ AU\ Deficit / Time\ to\ Hard\ Exit$ |
| **OCC** | Irreducible binary error-correction code (not arbitrary heuristic) |
| **Spatial Boundary** | Equilibrium envelope = compression containment field |
| **Pathway Acceptance** | Structural Close + Temporal Hold (wicks=reject, closes=commit) |
| **Liquidity** | Geometric endpoint of resolution (byproduct, not fundamental) |
| **Variance** | Residue from false assumptions, not randomness |
| **Time Quantization** | Intrinsic — institutional execution cycles (Asian/Activation/Hard Exit) |
| **Deep State** | Terminal coordinate = 100% deficit satisfaction |

## AGENT ARCHITECTURE — FINAL DIRECTIVE

The agent is a **computable finite-state machine** tracking recursive deficit resolution:
- Wicks = probes (reversible)
- Closes = commitments (irreversible)
- Pullbacks = friction-clearing (rebalance)
- Stop-outs = pathway rejections (shifts)

*Ontology fully mapped. Ready for logic gate formulation.*
- [ ] Section 4: [Pending]
- [ ] Section 5: [Pending]

---

## ONTOLOGY LAYER 1 — LOCKED

**Core axiom:** The Atomic Unit (AU) supersedes Fibonacci approximations as the ground-truth spatial coordinate for the Density Zone. Price action is not patterns — it is the sequential execution of quantized energy states.

**Structural law (scale-invariant):** $Impulse \rightarrow Rebalance \rightarrow Continuation$

**Dependency chain:** Distribution Field → Atomic Structure → Tier → AU → Impulse → Density Zone → Cascade

**Key mappings to DMR mechanics:**
| Ontology Concept | DMR Mechanical Equivalent |
|-----------------|--------------------------|
| Constraint deficit | Daily imbalance requiring resolution |
| Compression (Tier) | Asian Range classification |
| AU step-size | P90 body (min threshold for activation) |
| Impulse | P90 bar — directional breakout |
| Density Zone | Deep State retracement entry |
| Cascade | Sequential hourly entries until resolution |
| 80% terminal distribution | Kill switch / session completion |
| Violous state change | TP move back to activation |

---

*Auto-appended as sections are received.*


## CLOSING STATUS
Ontology Sections 1-3 complete (30 Q&As). Ready for logic gate formulation.




---

## SECTION 4: COMPUTABLE MECHANICS (Architecture Layer)

### CONTEXT (MAD's framing)

Goal now: reduce ontology into computable mechanics. Every abstraction must become measurable, updateable, falsifiable, executable. No more philosophy � precision only.

---

### SECTION 1 � AU MATHEMATICS

**Q1: How EXACTLY is AU derived?**
Is AU fixed per instrument? dynamically derived from Asian range? tier-dependent? volatility quantized? statistically inferred?
Need exact derivation chain. Example: Asian Range -> Tier -> AU?
*[Response pending]*

**Q2: Can AU change intraday?**
Or once initialized: is the AU invariant for the session?
*[Response pending]*

**Q3: What EXACTLY defines Tier 1 vs Tier 2 vs Tier 3?**
Need mathematical thresholds, transition boundaries, quantization rules.
*[Response pending]*

**Q4: Is tier discrete or continuous?**
Can a field exist between T1 and T2? Or must it snap into a discrete class?
*[Response pending]*

### SECTION 2 � TEMPORAL STRUCTURE

**Q5: What EXACTLY determines activation windows?**
Why 3 AM? Why 12 PM? Need causal explanation, not observational.
*[Response pending]*

**Q6: Does temporal pressure increase linearly, exponentially, logarithmically, or stepwise as time decays?**
Need actual pressure geometry.
*[Response pending]*

**Q7: What happens structurally at 12 PM?**
Does deficit freeze? redistribution begin? engine terminate? unresolved deficits roll forward?
*[Response pending]*

### SECTION 3 � PATHWAY MECHANICS

**Q8: What EXACTLY is a pathway?**
Is it: directional route? AU sequence? accepted vector chain? resolution geometry?
*[Response pending]*

**Q9: How many valid pathways can exist simultaneously?**
One? Few? Infinite constrained?
*[Response pending]*

**Q10: Can two pathways compete simultaneously?**
Bullish macro deficit vs bearish local route? Or does field collapse into single active pathway dominance?
*[Response pending]*

### SECTION 4 � REVERSAL MECHANICS

**Q11: What EXACTLY defines failed acceptance?**
Need exact conditions, mathematical invalidation.
*[Response pending]*

**Q12: When a pathway fails, does the field preserve deficit magnitude, recalculate, expand, or compress?**
*[Response pending]*

**Q13: Can deficits compound?**
Can unresolved deficits stack recursively? Or only one active macro deficit?
*[Response pending]*

### SECTION 5 � COMPLETION MECHANICS

**Q14: What EXACTLY constitutes completion?**
Target hit? temporal satisfaction? pressure collapse? AU exhaustion?
*[Response pending]*

**Q15: Can completion occur without full target traversal?**
Can the field terminate early if pressure dissipates?
*[Response pending]*

**Q16: What causes deep state overshoot?**
When price exceeds expected completion: new deficit creation? residual momentum? delayed compression release?
*[Response pending]*

### SECTION 6 � OBSERVATION THEORY

**Q17: What role does timeframe actually play?**
Is timeframe real? observational? computational convenience? resonance layer?
*[Response pending]*

**Q18: Can the entire system theoretically operate tickless?**
Pure event/state transitions without candles?
*[Response pending]*

**Q19: Are candles fundamental objects or merely observer compression artifacts?**
*[Response pending]*

### SECTION 7 � AGENT ARCHITECTURE

**Q20: What MUST the agent know at ALL TIMES?**
Minimal invariant state set: current tier? current AU? current deficit? current pathway? current pressure? current acceptance status? Need exact list.
*[Response pending]*

**Q21: What MUST NEVER be delegated to probabilistic inference?**
What parts are hard deterministic constraints?
*[Response pending]*

**Q22: What SHOULD remain probabilistic?**
Need exact boundary.
*[Response pending]*

**Q23: What is the SMALLEST viable version of the engine?**
If stripped to bare minimum: what 3-5 components reproduce the ontology?
*[Response pending]*

**Q24: What would PROVE the ontology false?**
True falsification condition. Not execution failure. Theory failure.
*[Response pending]*

**Q25: Final compression:**
Complete precisely: A market move exists because ____________________
One sentence. No metaphor. No trader language.
*[Response pending]*


---

## SECTION 4: COMPUTABLE MEANICS � RESPONSES

### SECTION 1 � AU MATHEMATICS

**Q1: How EXACTLY is AU derived?**

Statistically inferred via unsupervised machine learning, then dynamically locked per session.

Exact derivation chain:
1. Calculate Asian Session Range (19:00-03:00 EST) for historical data
2. Run K-Means clustering (k=3) on the distribution to identify natural volatility regimes (Centroids)
3. Tier boundaries are set at the mathematical midpoints between these centroids
4. AU = Cluster Centroid x 0.50

AU is exactly 50% of the mean Asian Range for that specific volatility group. It is the smallest unit of complete constraint resolution the field can execute.

**Q2: Can AU change intraday?**

Invariant by default, but subject to discrete intraday state change via tier shift. If the session's first impulse exceeds the next tier's trigger threshold (AU x 1.20 of the higher tier), the field intraday-reclassifies. The AU instantly snaps to the shifted tier's AU. Otherwise, it remains invariant until the 12 PM hard reset.

**Q3: What defines Tier 1 vs Tier 2 vs Tier 3?**

Discrete mathematical thresholds derived from K-Means cluster boundaries.
Example (EUR/USD):
- T1: Asian Range < 20p (Centroid ~19p)
- T2: Asian Range 20p-30p (Centroid ~25p)
- T3: Asian Range 30p-45p (Centroid ~38p)
- >45p: Deficit too wide; structural coherence collapses

**Q4: Is tier discrete or continuous?**

Strictly discrete. The field must snap into a discrete class. There is no 'between' state. The Asian Range mathematically forces the field into one of three quantized energy states, which dictates the exact AU step-size.

### SECTION 2 � TEMPORAL STRUCTURE

**Q5: What determines activation windows?**

Institutional execution cycles (Intrinsic Temporal Quantization):
- Asian Session (19:00-03:00 EST): The spatial compression engine. Accumulates constraint deficit.
- Activation Window (03:00-12:00 EST): The temporal release valve. Field permitted to break equilibrium.
- 12:00 PM EST: Hard structural boundary. Daily resolution engine shuts down due to depletion of primary activation energy.

**Q6: Temporal pressure geometry?**

Hyperbolically / Asymptotically.
Formula: Temporal Pressure = Remaining AU Deficit / Time to Hard Exit
As time approaches zero, pressure approaches infinity. Forces either violent resolution (Monolith) or structural stall.

**Q7: What happens at 12 PM?**

Engine termination and Full State Reset. Deficit does not roll forward. Daily constraint resolution window closes. All active pathways severed, all positions force-closed, field returns to default Compression until next 19:00 EST cycle.

### SECTION 3 � PATHWAY MECHANICS

**Q8: What EXACTLY is a pathway?**

An accepted vector chain / resolution geometry. It is the directional route defined by the initial impulse, validated by OCC, and traversed via sequential AU completions. Physical manifestation of field harvesting liquidity to satisfy deficit.

**Q9: How many valid pathways simultaneously?**

Single active pathway dominance. Field collapses into exactly one accepted vector chain at a time.

**Q10: Can two pathways compete simultaneously?**

No. Deficit is the constant; direction is the chosen pathway. If bullish macro deficit exists but bearish local route is accepted, the field is resolving the exact same deficit via the opposite vector. Opposing vectors are either probes (rejections) or the dominant pathway. Field never sustains competing pathways.

### SECTION 4 � REVERSAL MECHANICS

**Q11: What defines failed acceptance?**

The 80% Close Invalidation Rule and OCC Extreme Breach:
1. If any M5 candle closes past 80% of the initial impulse leg = pathway rejected
2. If M5 candle closes beyond the exact OCC Extreme (Zero-Buffer SL) = temporal acceptance voided
Wicks are probes (reversible). Closes are commitments (irreversible).

**Q12: When pathway fails, what happens to deficit?**

Preserves deficit magnitude. Field never abandons deficit. Pathway Reassignment: reverses to harvest opposite vector, resolving exact same spatial deficit.

**Q13: Can deficits compound?**

No. Only one active macro deficit per session (the Asian Range constraint deficit). Micro-deficits (AU loops) are fractional resolutions of the single macro deficit. They do not stack. At 12 PM, unresolved portion is terminated, not rolled forward.

### SECTION 5 � COMPLETION MECHANICS

**Q14: What constitutes completion?**

Target hit (AU/Tier extension reached) OR Temporal satisfaction (12 PM hard exit).

**Q15: Can completion occur without full target traversal?**

Yes. Via Temporal Satisfaction or Deep State Stall. If activation energy depletes before spatial target, session is functionally complete.

**Q16: What causes deep state overshoot?**

Residual momentum and Harvest mechanics. Field sweeps opposing order flow to fuel final kinetic push before structural wall terminates the move.

### SECTION 6 � OBSERVATION THEORY

**Q17: What role does timeframe actually play?**

Observational compression / Resonance layer. Physics are event-driven. M5 is the temporal resolution at which institutional execution cycles (OCC validation, AU completion) are optimally bucketed.

**Q18: Can system operate tickless?**

Yes. Pure event/state transitions: Compression -> Impulse Breach -> OCC Validation -> Rebalance -> AU Completion. Ticks are irrelevant noise; only structural events matter.

**Q19: Are candles fundamental or artifacts?**

Observer compression artifacts. Fundamental objects are structural events (Impulse, OCC, AU Target). Candles merely slice continuous events into 5-minute temporal buckets for human/algorithmic parsing.

### SECTION 7 � AGENT ARCHITECTURE

**Q20: What MUST the agent know at ALL TIMES? (Minimal invariant state set - 6 variables)**
1. Base Tier & AU (from 19:00-03:00 Asian Range)
2. Current Deficit (distance remaining to Tier Target)
3. Active Pathway Direction (bias lock from first band break)
4. Impulse Extreme & 80% Kill Zone (for invalidation tracking)
5. Temporal Pressure (time remaining to 12 PM Hard Exit)
6. Acceptance Status (has OCC validated the current vector?)

**Q21: What MUST NEVER be probabilistic? (Hard deterministic constraints)**
- Asian Range calculation & K-Means Tier classification
- 80% Close Invalidation Rule
- 12:00 PM EST Hard Exit
- Zero-Buffer OCC Extreme (SL)
- AU Target (50% of Centroid)
These are binary structural laws. No confidence intervals. Probabilistic weighting here introduces fatal architecture drift.

**Q22: What SHOULD remain probabilistic?**
- Regime confirmation (9 AM checkpoint boost)
- Pathway selection (which direction breaks the band first)
- Exact temporal duration of Rebalance phase (Goldilocks zone timing)
- Gear Shift activation likelihood
These are pathway variables within the valid solution space, not structural laws.

**Q23: Smallest viable engine (4 components)**
1. Deficit Calculator: Asian Range -> Tier -> AU
2. Impulse & 80% Tracker: State machine for vector validation
3. OCC State Machine: Search -> Wait_OPP -> In_Trade
4. Execution & Reset Engine: AU Target / 12 PM Exit -> Loop Reset

Stripped to these 4 modules: 89% WR, 1.92R expectancy across validated datasets. Everything else is optimization, not core physics.

**Q24: What would PROVE the ontology false?**

True falsification conditions:
- If the system fails to produce >90% structural coherence hit rate across uncorrelated asset classes (Forex, Crypto, Indices) when strictly filtering for 32-50% Goldilocks rebalance and OCC validation
- If the market resolves in continuous random walks rather than discrete quantized steps
- If deficits are proven to be abandoned rather than reassigned
- If the entire Atomic Structure ontology collapses under strict mechanical execution

**Q25: Final compression**

A market move exists because a constraint deficit must be resolved through the path of least structural resistance within the given temporal boundary, and the market recursively processes this deficit through quantized displacement steps until all outstanding orders are temporally satisfied or the activation window terminates.

---

## ONTOLOGY COMPLETE � ALL SECTIONS LOCKED

- Section 1: Atomic Structure (Q1-Q10)
- Section 2: State Transitions (Q1-Q10)
- Section 3: Field Mathematics (Q1-Q10)
- Section 4: Computable Mechanics (Q1-Q25)

**Total: 55 Q&As across 4 layers. Ready for logic gate formulation and code generation.**



---

## APPENDIX: AGENT CONTEXT & IMPLEMENTATION INSIGHTS

*Supplemental clarity from MAD for agent logic formulation. Not new ontology � context for implementation.*

---

### KEY INSIGHT 1: Single Active Pathway Dominance

No competing pathways. The field oscillates, probes, and searches until ONE vector achieves temporal acceptance (OCC + 80% hold). Until acceptance, the field is in SEARCH state. Multiple probes can occur; only one commits. This is the core of the OCC State Machine design.

### KEY INSIGHT 2: AU Intraday Reclassification

AU is INVARIANT by default. Tier shift only occurs on first impulse. If first impulse exceeds 1.20 x AU of next tier, AU snaps immediately. This is a ONE-TIME intraday recalculation � not continuous. Architecture implication: calculate Tier/AU at 3 AM, validate at first impulse, then lock for session.

### KEY INSIGHT 3: 12 PM Deficit Termination (NOT Roll Forward)

Unresolved deficits at 12 PM are TERMINATED, not compounded into next day. Each day is an isolated resolution window. This simplifies agent memory � no inter-day state carryover needed.

### KEY INSIGHT 4: Candles as Implementation Artifacts

Agents should internally operate on EVENTS (Band Break, OCC Close, 80% Close, AU Hit) and treat candles as the API layer only. Feed can be tick data; agent compresses to events internally. This eliminates timeframe dependency.

### KEY INSIGHT 5: Deterministic vs. Probabilistic Boundary (CRITICAL)

| NEVER Probabilistic (Hard Law) | Probabilistic OK |

|-------------------------------|------------------|

| Asian Range / K-Means Tier | 9 AM checkpoint boost |

| 80% Close Rule | Which direction breaks first |

| 12 PM Hard Exit | Rebalance duration |

| OCC Extreme (Zero-Buffer) | Gear Shift likelihood |

| AU Target | Regime confirmation |

If any hard law is put behind a probability filter, the entire architecture drifts and loses edge.

### KEY INSIGHT 6: Backtest Variance as Falsification Proof

The backtest doesn't just validate � it FALSIFIES. If any structural law was wrong, the variance would be broad and unpatterned. The fact that failures have exact anatomy (known when, where, what follows) proves the laws are structurally correct. The 2-5% miss rate = spread/slippage/news only. Not structural failure.

### KEY INSIGHT 7: Minimum Viable Engine Validation

Stripped to 4 modules: 89% WR, 1.92R expectancy across validated datasets. This means the core physics work. Everything else (P90 overlays, regime filters, cross-pair symmetry) is signal amplification, not core. Build the 4 modules FIRST. Add amplification only after core is validated.


---

## APPENDIX B: PYTHON REFERENCE IMPLEMENTATION

*CEREBUS Resolution Engine - Definitive State Machine Logic*

### State Machine Overview

1. STATE: SEARCH
 - Track running high/low from swing_origin
 - If move >= Tier Trigger (AU x 1.20), lock impulse_extreme, calculate 80% Kill Switch
 - Transition to WAIT_RETRACE

2. STATE: WAIT_RETRACE
 - Kill Switch: If M5 close breaches 80% level, pathway rejected. Reset to SEARCH.
 - Structural Penetration: Wait for pullback >= 1 AU OR 38.2%-50% Fib Zone
 - Transition to WAIT_OCC

3. STATE: WAIT_OCC
 - Wait for Opposite Candle Close (OCC) (candle closing back in impulse direction)
 - Execute Trade: Enter at close
 - SL: Exact impulse_extreme (Zero Buffer)
 - TP: Entry +/- 1 AU
 - Transition to IN_TRADE

4. STATE: IN_TRADE
 - Monitor for TP (wick or close) or SL (close-only)
 - Upon exit: swing_origin = exit price, return to SEARCH

---

### Key Implementation Rules

- AU is the Absolute Anchor: pullback_pips >= au_pips check
- Fib as Confluence: 0.382 <= retrace_pct <= 0.500 (secondary validation, AU+Fib overlap = ~85% of clean T1/T2 loops)
- Zero-Buffer SL: sl_px = impulse_extreme (no arbitrary buffers)
- TP Check: wick OR close
- SL Check: CLOSE-ONLY invalidation
- Continuous Loop Reset: _reset_state sets swing_origin to exit price immediately
- NO indicators, NO moving averages, NO discretionary logic
- Pure finite-state machine tracking recursive deficit resolution

### Architecture Validation Notes from MAD

- The Density Zone (DZ) is merely a statistical shadow (0.80x to 1.20x multiplier) approximating AU overlap
- AU is the structural ground truth
- Fibonacci levels (38.2%-50%) are the macro-confluence layer that aligns with AU on clean loops
- Stripping away DZ approximation and anchoring strictly to Exact AU Retracement + Fib Confluence simplifies agent logic and increases structural precision
- Tier Config Example (EUR/USD):
 - T1: max_ar=20p, au=10p, trig=12p
 - T2: max_ar=30p, au=12p, trig=15p
 - T3: max_ar=45p, au=15p, trig=19p


---

# CEREBUS FX v4 � COMPREHENSIVE ONTOLOGY OVERVIEW

*Precursor document: Agent-level summary before deep extraction layers.*

---

## FOUNDATIONAL PREMISE

The market exists in exactly ONE state: **Constraint Resolution.**

At all times, price is seeking to resolve outstanding orders within the given temporal boundary. What traders perceive as multiple states (compression, expansion, ranging, trending) are NOT independent states. They are **expressions, pathways, and characteristics** of a single resolution engine operating at different frequencies.

**The market is fundamentally a constrained temporal-spatial resolution engine that recursively processes directional displacement deficits through quantized atomic units until all outstanding orders are temporally satisfied.**

---

## CORE ONTOLOGY LAYERS (Summary)

### Layer 1: Distribution Field
- Highest-order pressure law
- Temporal completion enforcement
- Constrained resolution pressure
- Unresolved completion through time

### Layer 2: Atomic Structure
- **Atomic Unit (AU):** Minimum stable structural resolution quantum. NOT a measurement tool. Defines the Density Zone boundary with mathematical precision.
- **Density Zone:** Exact spatial coordinate (defined by AU overlap) where kinetic energy of impulse converts to potential energy of next AU. Fibonacci ratios (38.2%-50%) approximate this; AU IS the ground truth.
- **Tier:** Volatility quantization state. Determines AU behavior and valid propagation amplitude. Derived from Asian Range via K-Means clustering (k=3). AU = 50% of cluster centroid.
- **Cascade:** Recursive completion chaining. Large deficits resolve through sequential quantized AU loops (Impulse ? Rebalance ? Continuation). Cannot fail � only shifts expression.
- **Deep State:** Terminal spatial coordinate (~200% extension) where the distribution curve terminates. 100% deficit satisfaction.

### Layer 3: State Transitions
True state: **Resolution Construction** (always active)

Four expressions (NOT states):
1. **Compression:** Deficit accumulation under containment (Asian Session)
2. **Impulse:** Vector initiation via boundary violation (Tier Threshold Breach)
3. **Rebalance:** Friction clearing and pathway reloading (Density Zone / OCC validation)
4. **Completion:** Local deficit satisfaction (AU target hit)

Engine flow: Deficit ? Compression ? Saturation ? Impulse ? Rebalance ? Completion ? (recursive)

### Layer 4: Computable Mechanics
- **AU Derivation:** Asian Range ? K-Means (k=3) ? Tier boundaries at midpoints ? AU = centroid x 0.50
- **Temporal Pressure:** Remaining AU Deficit / Time to Hard Exit (hyperbolic)
- **OCC (Opposite Candle Close):** Binary structural filter. Irreducible error correction code. 66% of false continuations filtered out. Discovered via exhaustive backtest (6,814+ loops). Zero-Buffer protocol: R-multiple +83% vs buffered approach, 89-95% accuracy.
- **Institutional Time Quantization:** NOT emergent � intrinsic. Asian (19:00-03:00) compresses, Activation (03:00-12:00) releases, 12:00 PM terminates.
- **Failed acceptance:** 80% Close Invalidation Rule OR OCC Extreme breach. Pathway rejected, deficit preserved, opposite vector initiated.
- **Completion:** Target hit OR temporal satisfaction (12 PM hard exit).
- **12 PM Reset:** Engine terminates. Deficits do NOT roll forward. No inter-day state carryover.


---

## AGENT ARCHITECTURE OVERVIEW

### What the Agent IS
- A recursive deficit-resolution tracker
- A finite-state machine tracking structural state transitions
- NOT a predictive model, NOT intuition-based, NOT narrative-driven

### What the Agent Does NOT Do
- Predict direction or magnitude
- Read candles as patterns or setups
- Use indicators, moving averages, or discretionary logic
- Treat any hard structural law as probabilistic

### Minimal Invariant State (6 Variables Agent Must Know at ALL TIMES)
1. Base Tier & AU (from 19:00-03:00 Asian Range)
2. Current Deficit (distance remaining to Tier Target)
3. Active Pathway Direction (bias lock from first band break)
4. Impulse Extreme & 80% Kill Zone (for invalidation tracking)
5. Temporal Pressure (time remaining to 12 PM Hard Exit)
6. Acceptance Status (has OCC validated the current vector?)

### Hard Deterministic Constraints (NEVER Probabilistic)
- Asian Range calculation & K-Means Tier classification
- 80% Close Invalidation Rule
- 12:00 PM EST Hard Exit
- Zero-Buffer OCC Extreme (SL)
- AU Target (50% of Centroid)

### Probabilistic Variables (Statistical Weighting OK)
- Regime confirmation (9 AM checkpoint boost)
- Pathway selection (which direction breaks first)
- Exact temporal duration of Rebalance phase (Goldilocks zone timing)
- Gear Shift activation likelihood

### State Machine: 4 States
**SEARCH ? WAIT_RETRACE ? WAIT_OCC ? IN_TRADE**

| State | Trigger | Exit |
|-------|---------|------|
| SEARCH | Impulse >= Tier Trigger (AU x 1.20) | Transition to WAIT_RETRACE |
| WAIT_RETRACE | 80% Kill Switch breach (close) OR 1 AU / Fib 38.2-50% penetration | Reset to SEARCH or transition to WAIT_OCC |
| WAIT_OCC | Opposite Candle Close confirmed | Entry signal, transition to IN_TRADE |
| IN_TRADE | TP hit (wick/close) OR SL hit (close only) | Reset: swing_origin = exit, return to SEARCH |

### Zero-Buffer Protocol
- SL = exact impulse_extreme (no arbitrary buffers)
- TP = entry +/- 1 AU
- Entry = close of OCC candle
- Validation: 6,814+ backtest loops, +83% R-multiple vs buffered, 89-95% accuracy

### Key Mappings: Ontology ? Mechanics
| Ontology Concept | Mechanical Equivalent |
|-----------------|----------------------|
| Constraint Deficit | Daily Asian Range imbalance |
| Compression | Asian Session (19:00-03:00 EST) |
| AU Step-size | P90 body minimum threshold |
| Impulse | Boundary breach >= Tier Trigger |
| Density Zone | 38.2-50% Fib / AU overlap entry zone |
| Cascade | Sequential AU loop entries until completion |
| 80% threshold | Kill switch / pathway invalidation |
| Rebalance | Pullback to Density Zone + OCC |
| Completion | AU target hit OR 12 PM hard exit |
| Deep State | 200% extension = terminal coordinate |
| OCC | Binary validation anchor (close-only) |


---

## FILE INDEX � CEREBUS ONTOLOGY SUITE

| File | Purpose |
|------|---------|
| cerebus_forward.md | Foundational Ontology Forward � agents read this FIRST |
| cerebus_qa_recap.md | Complete Q&A Recap � all sections consolidated for agent ingestion |
| cerebus_resolution_engine.py | Definitive Python Reference � 4-state FSM implementation |
| manual_ontology.md (this file) | Layered deep ontology � Appendix A (insights) + Appendix B (architectural notes) |

