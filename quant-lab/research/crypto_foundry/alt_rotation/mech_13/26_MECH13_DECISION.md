# CRYPTO-ALT-MECH-13 — DECISION

## Verdict

**PASS_MECH13_LIFECYCLE_DEEPENING_WITH_LIMITATIONS**

MECH-13 deepens MECH-12's objects: state lifecycle stage profiles, per-cell
mass-migration laws, failure-birth -> initiation geometry with a
necessity/sufficiency audit, a deep constraint-entropy map with driver and
propagation tests, an activation-capacity x resolution-entropy 2x2, waterfall
subtype matrix and activation threshold surfaces, patch response curves with
heterogeneity, a metastability **confirm-or-kill = kill** recheck, full
absolute x sigma shock geometry and materiality audit, and a full directional
asymmetry atlas with upside/downside deepening.

The most important outputs are NEGATIVES that correct earlier checkpoints:
**(1) no 4-state cell is metastable** under a proper baseline (killing
MECH-12's HH-metastable-like label), and **(2) initiation has no necessary
single coordinate** — it is a loose multi-coordinate system.

---

## Key results

### The two headline findings

1. **Metastability killed.** Against a shuffled-baseline self-transition
   excess, all four cells classify ORDINARY_STATE (excess ~0, dwell 2-3D).
   MECH-12's "HH metastable-like" reflected relative dwell z-scores
   confounded by large stationary shares. No cell is a true attractor; the
   metastability node is DISSOLVED and no transfer-operator escalation is
   warranted.

2. **Initiation is multi-coordinate, not primitive.** 17/28 birth-coordinate
   comparisons separate success at FDR in all four cells, but the
   leave-one-out audit finds **0 NECESSARY / 5 SUFFICIENT / 4 CONDITIONAL /
   7 SUBSTITUTABLE / 12 REDUNDANT**. Removing any single birth coordinate
   rarely drops the success-model AUC below ~0.80. There is no necessary
   initiation primitive — consistent with the Modeling Bible's
   loose-global-constraint doctrine.

### Supporting findings

- **Lifecycle**: HH matures into compression (dispersion 1.96->0.85) and
  delayed delivery (fwd7 prop 0.24->0.67, reentry 0.39->0.10); each cell has
  a distinct mass law. MECH-11's competing-risk mass-shift reproduces.
- **Constraint entropy**: entropy collapse is real (HH 1.15->0.31 bits);
  driver is global (vol/breadth/dispersion at FDR); compression is
  synchronized (WEAK_LOCAL_COLLAPSE), not a spatial cascade.
- **Spatial/temporal constraint axes are independent** (rho=-0.02): broad
  activation raises propagation, resolution-entropy presence lowers it, and
  HIGH_ACT+HIGH_ENT is the crowded-unresolved worst state.
- **Waterfall stays a threshold hierarchy** — ORDERLY_SHALLOW_TO_DEEP is the
  only named subtype (n=240); 19/35 monotonic activation surfaces.
- **Patch responses saturate** (124/140) and are homogeneous across patches.
- **Absolute x sigma are independent additive axes** (abs +0.012, sigma
  +0.016 AUC); extreme-but-reverted shocks are often economically trivial
  (materiality primitive).
- **Directional asymmetry confirmed**: upside is field-selective (broad vs
  isolated upstream breadth 0.58 vs 0.16), downside is field-neutral.
- **Directional info gain is local** — only mature HH narrows next-direction
  entropy (0.87->0.64 bits).
- **Local conversion paths generalize** into PATH_A/C/D (non-circular PATH_A
  lift 1.41x), not a single primitive.

## Node actions

- PROMOTE: LIFECYCLE_DEEP_MAP, STATE_MASS_MIGRATION, INITIATION_GEOMETRY,
  INITIATION_PRIMITIVE_AUDIT, ENTROPY_DEEP_MAP, ENTROPY_PRIMITIVE_AUDIT,
  SPATIAL_TEMPORAL_CONSTRAINT_MATRIX, WATERFALL_SUBTYPE_MATRIX,
  ACTIVATION_THRESHOLD_SURFACES, PATCH_RESPONSE_CURVES, ABSOLUTE_SIGMA_SHOCK,
  SHOCK_MATERIALITY, DIRECTIONAL_ASYMMETRY_ATLAS, UPSIDE_GEOMETRY,
  LOCAL_CONVERSION_PATHS.
- DESCRIPTIVE: ENTROPY_PROPAGATION, RESPONSE_CURVE_HETEROGENEITY,
  DOWNSIDE_GEOMETRY, DIRECTIONAL_INFORMATION_GAIN.
- **DISSOLVE: METASTABILITY_RECHECK** (0 cells survive baseline test).

## What this means for the broader field map

- The field reads as a **constraint system with differential activation
  thresholds under common forcing** (waterfall thresholds, homogeneous
  patch responses, independent spatial/temporal axes, synchronized entropy
  compression) — NOT a grammar of stable sequences and NOT a set of
  metastable basins.
- Direction is only locally constrained (mature HH); upside is field-gated,
  downside is field-neutral.
- A future research layer should build from **birth-conditioned persistence**
  and **threshold/constraint geometry**, not from dwell-based metastability
  or sequence templates.

## Limits

- All ordering/initiation/entropy/asymmetry results are descriptive (<= L2).
- Metastability recheck is an empirical null; volatility stays intensity/
  retention context.
- No strategy translation performed.

`human_review_required = TRUE`
`next_checkpoint_authorized = FALSE`
NO STRATEGY · NO PNL · NO EXECUTION · NO SIZING · NO LEVERAGE · NO DEPLOYMENT