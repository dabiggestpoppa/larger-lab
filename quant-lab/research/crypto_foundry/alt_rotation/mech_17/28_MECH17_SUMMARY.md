# CRYPTO-ALT-MECH-17 — SUMMARY

**TRAFFIC LAW CARTOGRAPHY** — road freeze audit, transfer-law family
decomposition, broad forcing variants, capacity & congestion, threshold bands,
saturation anatomy, exit pressure / entropy by regime, birth-trajectory
geometry, 2022 structural shift, adaptive-law role assignment, Field Model v1
freeze preparation.

AGENT 1 — CANONICAL FIELD CARTOGRAPHER · terrain research only
(parents: MECH-16 d585ab32; MECH-15, MECH-14)

---

## Headline verdicts

- Road topology freeze audit: FREEZE_CANDIDATE (4-state 0.67 / 6-cell 0.77 /
  8-cell 0.90; local highways 0.94).
- Forcing decomposition: MULTI_FORCING_FAMILY (PCA1 0.234, mean |Spearman|
  0.149 → not a single latent forcing; single scalar dissolved).
- Capacity: measurable as state-local response ceiling (0.51–1.10).
- Congestion: real but weak/state-local (pool rho vs reentry +0.105, vs prop
  ≈0.02).
- Exit pressure: robust; discriminating departing-exit bands.
- Transfer efficiency: exists, state-local and drifting.
- Threshold bands: transport better than point thresholds (drift/width
  slack 0.10–0.20).
- Saturation shape: stable family (logistic≈hill≈linear, ΔRMSE <0.001);
  nodes drift; **2022 is a strong node/shape outlier.**
- Hysteresis: present as pilot (rising-falling gap +0.16..+0.29, p≈0), PARKED.
- Birth: better as trajectory (4 stages, coefficients vary by stage);
  equifinality only partly earned (1 qualified archetype).
- Aborted formations diverge at INITIATION (higher demand + higher exit-
  entropy when aborting).
- 2022 window: response-shape shift (H2-lean), not a permanent regime.
- Field Model v1 GLOBAL freeze: FREEZE_PARTIAL_GLOBAL.

## Final-question answers

1. **Road topology ready to freeze?** YES — 6/8-cell roads + state base +
   local highways freeze as topology; no contradiction found.
2. **Minimum traffic-law objects?** SEVEN: TRAFFIC_DEMAND, CAPACITY,
   CONGESTION, EXIT_PRESSURE, TRANSFER_EFFICIENCY, SATURATION_LAW,
   THRESHOLD_BAND (03_TRAFFIC_OBJECT_DEFINITIONS.csv).
3. **Multiple forcing species?** YES — MULTI_FORCING_FAMILY. Families are
   near-orthogonal (mean |corr| 0.149); PCA1 only 23.4%; stablecoin-capital +
   physical + BTC-anchor correlate with the old common scalar (0.60–0.74),
   dispersion & concentration-release are essentially orthogonal to it.
4. **Capacity measurable?** YES as state-local response ceiling (06 map);
   physical capacity units NOT supportable.
5. **Congestion a real object?** PARTIAL — state-local demand/capacity ratio
   exists and weakly associates with reentry; relationship to propagation muted.
6. **Exit pressure encodable robustly?** YES — departing-exit concentration /
   dominant-share bands (OPEN→NEAR_SINGLE_EXIT) are stable by regime.
7. **Transfer efficiency useful?** YES as ADAPTIVE/state-local coordinate, not
   a constant.
8. **Bands more stable than point thresholds?** YES — subperiod half-sat
   drift (1.6–4.7) is 10–20% of band width; use bands, re-fit edges.
9. **Saturation shape stable?** YES — heldout family RMSE differ by <0.001;
   monotonically-saturating family fits all patches.
10. **Nodes drift inside stable shape?** MOSTLY YES — except the 2022 window
    where the shape itself broke (slope ~0.13, ceiling clamped).
11. **Hysteresis / path dependence?** CANDIDATE present (PARKED).
12. **Birth as trajectory?** YES — stage geometry (precondition→initiation→
    commitment→survival) differs by stage.
13. **Multiple paths to same state?** PARTIAL — cluster0 (n=360) is a broad
    multi-subperiod path; cluster1/3 are period-local (2020-21, 2022) and fail
    the n/subperiod bar; so only 1 hardened archetype.
14. **What distinguishes aborted formations?** Higher INITIATION demand
    (+0.26 vs −0.09) and higher exit-entropy (0.70 vs 0.15) → aborted births
    initiate crowded & under less-committed exit conditions.
15. **2022 distinct law or extreme?** H2_LEAN / response-shape shift (saturation
    shallow + exits concentrated); NOT promoted to permanent regime.
16. **Non-invariants to ADAPTIVE_LAW?** ENTROPY_RESPONSE, COMMON_FORCING,
    THRESHOLD_HIERARCHY, BIRTH_GEOMETRY, SATURATION_LAW, TRAFFIC_DEMAND,
    TRANSFER_EFFICIENCY (22 map).
17. **GLOBAL component freeze-ready?** FREEZE_PARTIAL_GLOBAL — topology freezes;
    adaptive-law layer re-fitted (27 map).

## Limits

- Descriptive (<= L2); no strategy translation; no direction sign.
- Entropy×demand and congestion maps are descriptive interaction bars.
- SoSoValue / ETF-flow context remains DATA_BLOCKED (no local data).
- 2022 saturation ceiling used a clamped logistic (upper bound 1.1).

`human_review_required = TRUE`
`next_checkpoint_authorized = FALSE`
NO STRATEGY · NO PNL · NO EXECUTION · NO ENTRY/EXIT · NO SIZING · NO LEVERAGE · NO DEPLOYMENT
