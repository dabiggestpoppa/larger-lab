# MECH-3 AGENT-2 PROMOTION REVIEW (WORKSTREAM O)

**Role:** AGENT 1 — MAIN FIELD CARTOGRAPHER.
**Rule:** derivative findings may be proposed for canonical promotion only with a
clear data contract, sufficient observation layer, no PIT leakage, redundancy
addressed, perturbation survival, nulls documented, test counts reconciled, and
claim language matching causal evidence. Nothing enters the Canonical Terrain Bank
automatically.

## 1. Promotion inbox

- `quant-lab/research/crypto_foundry/derivatives/` was **empty at MECH-3 run start**.
- During the MECH-3 run, Agent 2 opened `derivatives/lower_field/` with
  `01_HYPOTHESIS.md`, `02_PREREGISTRATION.md`, `15_OBSERVATION_LIMITS.md`,
  `17_TEST_COUNT_RECONCILIATION.md` and a `DATA_TRUTH/raw/` collection of
  rank-2000 snapshots (data collection phase, 2020-06 onward).
- **No `PROMOTION_CANDIDATE.md` has been filed yet** (PROMOTION_CANDIDATES/ empty;
  REPORTS/ and RESULTS/ empty at review time). This review therefore evaluates the
  *preregistered hypothesis*, not results.
- Committed side-lane capture reviewed: `CRYPTO_LOWER_FIELD_HYPOTHESIS_CAPTURE.md`
  (`85030bc4`) — the lower-field (ranks 501+) response-geometry hypothesis.

## 2. Review of the lower-field hypothesis capture

| Criterion | Assessment |
|---|---|
| Data contract | Rank-501+ PIT universe being built by Agent 2 (rank-2000 snapshots in DATA_TRUTH/raw/) — not yet in the canonical bank |
| Observation layer | Partial — snapshots from 2020-06 onward; historical depth and dead/delisted preservation to be verified by Agent 2's own truth layer |
| PIT leakage | To be verified by Agent 2 (PIT snapshot methodology documented in its preregistration) |
| Redundancy | Not addressed (no results yet) |
| Perturbation | Not tested (no results yet) |
| Nulls | Documented as hypothesis and in its 15_OBSERVATION_LIMITS.md |
| Test counts | Its 17_TEST_COUNT_RECONCILIATION.md exists (preregistration stage) |

**Classification: DEFER (in progress, no candidate filed).** The hypothesis is
coherent and does not conflict with MECH-2's null (MECH-2's SMALL_CAP_ROTATION
null applies *inside* the Top-500; the hypothesis explicitly asks about
below-rank-500 behavior). The lane is in data-collection with its own
preregistration and observation-limits discipline. When a formal
`PROMOTION_CANDIDATE.md` is filed, this review will be re-run against the
promotion rule (§16 of 01_PREREGISTRATION.md) and the results evaluated on their
merits. Until then: **DEFER** — no automatic acceptance into canonical terrain.

## 3. Promotion rule applied to MECH-3's own candidates

MECH-3 does not self-promote; it produces canonical map updates via
`19_NEW_NODE_MERGE_DISSOLVE.csv`. Candidate primitives (WS J) are labeled
GLOBAL_CANDIDATE_PRIMITIVE / LOCAL_PRIMITIVE — these are *audit* classifications
for future formalization, not automatic promotions. VOLATILITY's candidate-primitive
status requires independent re-validation (e.g., by Agent 2 perturbing the
concentration-exit reconstruction with alternate vol proxies) before any canonical
promotion.

## 4. Invitations to Agent 2 (derivative lane)

Non-promotion, non-blocking questions surfaced by MECH-3 that belong in the
derivative lane:

1. Is the WS K Ethereum articulation point robust to edge-threshold variation and to
   alternative velocity horizons? (connectivity sensitivity)
2. Does the WS L hysteresis (entry-route → exit-route dependence) survive when
   entry/exit are defined on smoothed rather than same-day state series?
3. Are the 7 "liquidity island" chains (WS K singletons) genuinely decoupled, or an
   artifact of the |r| ≥ 0.50 threshold?
4. WS I release triggers (imp_share for P1, chain TVL for P2, top3-share for P3):
   do they recur with alternative plateau definitions?

None of these are accepted into canonical terrain until validated.
