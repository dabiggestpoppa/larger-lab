# 2026-06-02 Daily Notes

## ST Spread Buffer Fix (15:30 EDT)
- **Problem**: ST SL placed at OCC extreme with no spread buffer → tight SLs getting stopped out by spread widening
- **GBPAUD trade at 11:08**: OCC high was below entry, bridge clamped to 1pip above → instant stop-out
- **Fix**: Added `spread_buffer` to ST engine SL calc: `SL = OCC extreme + spread_buffer`
  - GBP crosses: 3p, JPY pairs: 2p, majors: 1.5p, metals: 15p
- **Also fixed**: ST executor validation too strict (`sl_r <= entry_r`), added 1-point tolerance
- **Files**: `quant-lab/engines/symmetry_trap.py`, `quant-lab/mt5/symmetry_trap_executor.py`

## Process Cleanup & Guardian v2 (16:00 EDT)
- **Bridge PID 14496** was running OLD engine code since June 1 — never restarted after fixes
- **Killed old bridge**, restarted with new engine code (verified `min_sl` and `spread_buf` in session init)
- **Guardian rewritten** as `cerebus_guardian.py` v2:
  - Monitors bridge + ST executor
  - Rate-limited restarts (5/hour max per process)
  - MT5 connection health check every 5 cycles
  - Critical vs non-critical process classification
- **Cron job** set up as backup guardian (every 5 min) — restarts guardian if it dies
- **Cleaned up**: 2x OCE backend, 4x telegram gateway, old guardian, old bridge
- **Final clean process list**:
  - PID 8960: cerebus_live_bridge.py (new, with fix)
  - PID 18484: cerebus_guardian.py v2
  - PID 19908: symmetry_trap_executor.py (no longer looping)
  - PID 21956: OCE backend (uvicorn :8000)

## MAD Away Mode
- MAD stepped away at ~16:00 EDT
- Processes self-monitoring via guardian + cron backup
- If anything dies, guardian restarts it, only alerts MAD if critical (bridge) or rate limit hit
