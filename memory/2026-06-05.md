# 2026-06-05 — Daily Notes

## Group Combinatorics Matrix — COMPLETE
- Built full combinatorics script: `quant-lab/scripts/group_combinatorics.py`
- Output: `quant-lab/reports/GROUP_COMBINATORICS.md`
- Top 3 per category at every size 2-14
- MAD's Phase 1 (low cost → $250) and Phase 2 (max profit at $250+) documented

## Low Cost Hex — CRITICAL FAILURE & FIX
- **FAILURE**: Bridge running Sign 7 pairs (EURUSD, USDJPY, CHFJPY, etc.) instead of Low Cost Hex
- **Root Cause**: cerebus_guardian.py had HARDCODED Sign 7 symbols in PROCESSES["bridge"]["args"]
  - When guardian/autopilot restarted, it passed old symbols to bridge, ignoring deploy_config.py
  - Bridge uses --symbols CLI arg if provided, NOT the DEPLOY_SYMBOLS from config
- **Evidence**: 
  - bridge.log 09:40:52 shows Sign 7 pairs starting
  - account_tracker shows CHFJPY.PRO and EURUSD.PRO trades (Sign 7 pairs)
  - deploy_config.py was correct, but guardian ignored it
- **FIX**: Updated cerebus_guardian.py to use EURJPY,EURNZD,GBPNZD,EURAUD,GBPAUD,GBPCAD
- **Additional**: symmetry_trap_executor.py still has hardcoded SYMBOL="EURUSD.PRO" — needs decommissioning

## Memory Integrity Warning
- I have MEMORY.md with all the lessons, but failed to apply them
- This is NOT a code bug — it's a workflow/operational failure
- Root cause: Not validating that running code matches intended config after restart

## System Status (03:27 EDT)
- Before fix: Running Sign 7 pairs via guardian hardcoded args
- After fix: Guardian updated, needs restart
- All 3 original fixes still verified
- London open approaching

## OpenClaw Runtime Event Additions

### Guardian Unicode Fix Verification
- **Status**: Already fixed. The cerebus_guardian.py code contains NO emoji characters.
- **Evidence**: guardian.log entries showing emojis (🚨 ✅) are FROM PREVIOUS VERSION before fix.
- **The guardian.py now uses clean ASCII logging only.**

### Hard Exit Verification (12 PM Cutoff)
- **Status**: CONFIRMED - `hard_exit()` is NEVER called in production.
- **Evidence**:
  - `symmetry_trap.py` line 693-701: `hard_exit()` method EXISTS
  - `ACTIVATION_END = time(12, 0)` constant exists (line 29) but is unused
  - `cerebus_live_bridge.py`: No calls to `hard_exit()` anywhere
  - Bridge processes only: ENTRY, TP_HIT, SL_HIT events
  - No time-based session termination in the live loop
- **Conclusion**: The Alien Edge is active - no 12 PM cutoff in production.

### Low Cost Hex Expected Trades
- **Calculated Expected Daily Trades**: 8.61 (per deploy_config.py expected values)
  - EURJPY: 0.35/day
  - EURNZD: 1.76/day
  - GBPNZD: 1.74/day
  - EURAUD: 1.01/day
  - GBPAUD: 1.74/day
  - GBPCAD: 2.01/day

### Signals Archive
- **Zombie data found**: signals.jsonl contains GBPCAD.PRO trade at 13:42-13:46 (Sign 7 era)
- **Action**: Need to archive and clear signals.jsonl for clean restart

### Current Bridge Status
- Running Low Cost Hex symbols since 14:10:04 EST (restart confirmed in bridge.log)
- Asian Range initialized correctly for all 6 pairs
- No trades yet due to session initialization timing