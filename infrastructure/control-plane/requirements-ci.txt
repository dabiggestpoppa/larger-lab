# OCE Book 2 control plane — pinned CI test dependencies (immutable contract).
# Do not silently upgrade during the repair cycle; bump deliberately.
pytest==9.0.3
psycopg2-binary==2.9.12
redis==4.6.0
fastapi==0.136.1
uvicorn==0.47.0
httpx==0.28.1
