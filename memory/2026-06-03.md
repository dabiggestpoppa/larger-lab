# 2026-06-03 Daily Notes

## EURUSD/USDCHF WR Discrepancy Investigation

### Finding
- Multi-asset run (5/31) showed EURUSD 85% WR, USDCHF 84.9% WR
- My per-asset backtest (6/2) showed EURUSD 44% WR, USDCHF 40% WR
- ONLY these 2 assets show the discrepancy — all other assets match between runs
- Trade count also differs: 1163 vs 886 (EURUSD), 1153 vs 847 (USDCHF)

### What I confirmed (no code changes, read-only investigation)
- Data files identical (same bars, same date range, same file sizes)
- Engine code (`symmetry_trap.py`) unchanged since 6/2 3:38 PM
- Both runs use same `SymmetryTrapBacktest` class
- SL placement is correct (above entry for SHORT, below for LONG)
- Using same code path gives same result — both produce 44%

### Theory timeline
1. 5/30 engine: `sl_price = impulse_extreme` (buggy — SHORT SL below entry = in profit direction, never hit)
2. 6/2 10:49 engine: `sl_price = OCC extreme` (no spread buffer)
3. 6/2 16:37 engine (current): `sl_price = OCC extreme + spread_buffer`

### Paradox
- If old engine had SHORT SL in profit direction, SHORT trades would NEVER hit SL → inflated WR
- But OLD engine's SL check for SHORT (`bar.close >= sl_price`) with sl_price below entry would actually trigger IMMEDIATELY (price already above impulse low)
- So old engine should give LOW WR for SHORT, not high 85%
- This means the multi-asset 85% WR was NOT from the old engine either — there's a third version or different code path

### Status: HANDED TO ARCH for resolution
- MAD stopped me after 3+ hours of investigation
- Created detailed report with test scripts (all in backtest/portfolio_matrix/)
- No engine files were modified during investigation

## MC-Pooled vs Full Backtest Validation
- MC pooling per-asset `per_trade_pnl` arrays gives IDENTICAL results to full portfolio backtest
- Delta = 0% on WR, PF, PnL, MaxDD — confirmed on 5-asset portfolio (GBPJPY, CHFJPY, GBPAUD, GBPNZD, NZDUSD)
- Data quality issue: 6 assets have `win_rate = 0` in their backtest JSON (save bug), but `per_trade_pnl` data is correct

## Portfolio Config Matrix (MAD directive)
- MAD wants full combinatorics bible: how many portfolio configs from 2 to all assets
- 14 assets with valid per_trade_pnl data (out of 19 in ASSET_CONFIGS; 5 have empty PnL arrays)
- Each asset: 4 states (off, P90-only, ST-only, both) = 4^19 = ~275 billion theoretical combos
- Min 2 assets: sum of C(19,k) for k=2..19 = 524,268 unique asset combinations
- With engine variants: ~275 billion total configs
- Practical: 2-5 assets = ~3.2M configs (testable), 6+ = greedy approach needed

### Matrix Generator
- Script: `quant-lab/backtest/portfolio_matrix/matrix_quick.py`
- Reduced to 2000 sims per combo for speed
- Subagent timed out at 30 min trying to run exhaustive 2-5 asset MC
- Need to run directly or split into smaller batches
- Greedy expansion from best 5-asset portfolio for sizes 6-14
- Output: `matrix_raw.json` + `PORTFOLIO_MATRIX.md`

### Active subagent: portfolio-matrix (d9229eef) — TIMED OUT
- Was running `run_matrix.py` (5000 sims, exhaustive 2-5 asset)
- Produced no output before timeout
- `matrix_quick.py` (2000 sims) not yet run

## Actions needed (post-Arch resolution)
1. Fix EURUSD/USDCHF WR issue
2. Run `matrix_quick.py` to generate portfolio matrix
3. Generate final portfolio thresholds once all 7 assets confirmed
4. Add results to Lab Bible / Obsidian vault

## Files created today (test scripts only — no engine/config modified)
- backtest/check_st_multi.py
- backtest/check_wr.py
- backtest/check_data.py
- backtest/test1_eurusd_direct.py
- backtest/test2_old_engine.py (incomplete)
- backtest/test3_old_engine.py (incomplete)
- backtest/debug_eurusd.py
- backtest/run_old_eurusd.py (incomplete)
- backtest/final_check.py
- backtest/mc_vs_backtest.py
- backtest/portfolio_matrix/asset_stats.py
- backtest/portfolio_matrix/mc_vs_backtest.py
- backtest/portfolio_matrix/run_matrix.py
- backtest/portfolio_matrix/matrix_quick.py
- backtest/portfolio_matrix/test_imports.py
- backtest/run_portfolio_mc_no_eu_uchf.py
- backtest/gen_thresholds_5assets.py
- engines/symmetry_trap_old.py (empty placeholder — for testing only)

## 5-Asset Portfolio MC Results (confirmed, stable)
Assets: GBPJPY + CHFJPY + GBPAUD + GBPNZD + NZDUSD
- Pooled: 3,687 trades | WR 88.4% | PF ~15 | Total PnL +36,546p
- MaxDD 95th: 109.1p | 99th: 132.8p | Worst: 205.4p
- Loss streak warning: 5 | Critical: 9
# 2026-06-03 Daily Notes

## EURUSD/USDCHF WR Discrepancy Investigation

### Finding
- Multi-asset run (5/31) showed EURUSD 85% WR, USDCHF 84.9% WR
- My per-asset backtest (6/2) showed EURUSD 44% WR, USDCHF 40% WR
- ONLY these 2 assets show the discrepancy — all other assets match between runs
- Trade count also differs: 1163 vs 886 (EURUSD), 1153 vs 847 (USDCHF)

### What I confirmed (no code changes, read-only investigation)
- Data files identical (same bars, same date range, same file sizes)
- Engine code (`symmetry_trap.py`) unchanged since 6/2 3:38 PM
- Both runs use same `SymmetryTrapBacktest` class
- SL placement is correct (above entry for SHORT, below for LONG)
- Using same code path gives same result — both produce 44%

### Theory timeline
1. 5/30 engine: `sl_price = impulse_extreme` (buggy — SHORT SL below entry = in profit direction, never hit)
2. 6/2 10:49 engine: `sl_price = OCC extreme` (no spread buffer)
3. 6/2 16:37 engine (current): `sl_price = OCC extreme + spread_buffer`

### Paradox
- If old engine had SHORT SL in profit direction, SHORT trades would NEVER hit SL → inflated WR
- But OLD engine's SL check for SHORT (`bar.close >= sl_price`) with sl_price below entry would actually trigger IMMEDIATELY (price already above impulse low)
- So old engine should give LOW WR for SHORT, not high 85%
- This means the multi-asset 85% WR was NOT from the old engine either — there's a third version or different code path

### Status: HANDED TO ARCH for resolution
- MAD stopped me after 3+ hours of investigation
- Created detailed report with test scripts (all in backtest/portfolio_matrix/)
- No engine files were modified during investigation

## MC-Pooled vs Full Backtest Validation
- MC pooling per-asset `per_trade_pnl` arrays gives IDENTICAL results to full portfolio backtest
- Delta = 0% on WR, PF, PnL, MaxDD — confirmed on 5-asset portfolio (GBPJPY, CHFJPY, GBPAUD, GBPNZD, NZDUSD)
- Data quality issue: 6 assets have `win_rate = 0` in their backtest JSON (save bug), but `per_trade_pnl` data is correct

## Portfolio Config Matrix (MAD directive)

### Combinatorics
- 14 assets with valid per_trade_pnl data (out of 19 in ASSET_CONFIGS; 5 have empty PnL arrays)
- Each asset: 4 states (off, P90-only, ST-only, both) = 4^19 = ~275 billion theoretical combos
- Min 2 assets: sum of C(19,k) for k=2..19 = 524,268 unique asset combinations
- With engine variants (3 states per selected asset: P90/ST/both): ~275 billion total configs
- Practical: 2-5 assets = ~3.2M configs (testable), 6+ = greedy approach needed

### Detailed by size (14 assets with data, 3 engine states each):
- 2-asset: C(14,2) x 3^2 = 91 x 9 = 819 configs
- 3-asset: C(14,3) x 3^3 = 364 x 27 = 9,828 configs
- 4-asset: C(14,4) x 3^4 = 1001 x 81 = 81,081 configs
- 5-asset: C(14,5) x 3^5 = 2002 x 243 = 486,486 configs
- 6+-asset: greedy expansion only (too many for exhaustive)

### Matrix Generator
- Script written: `quant-lab/backtest/portfolio_matrix/matrix_quick.py` (2000 sims)
- Subagent timed out at 30 min running exhaustive MC
- Status: NOT YET RUN successfully — memory flush is blocking file writes to workspace
- Output targets: `matrix_raw.json` + `PORTFOLIO_MATRIX.md`

## 5-Asset Portfolio MC Results (confirmed, stable)
Assets: GBPJPY + CHFJPY + GBPAUD + GBPNZD + NZDUSD
- Pooled: 3,687 trades | WR 88.4% | PF ~15 | Total PnL +36,546p
- MaxDD 95th: 109.1p | 99th: 132.8p | Worst: 205.4p

## Files created today (test scripts only — no engine/config modified)
- backtest/check_st_multi.py, check_wr.py, check_data.py
- backtest/test1_eurusd_direct.py, test2_old_engine.py, test3_old_engine.py
- backtest/debug_eurusd.py, run_old_eurusd.py, final_check.py
- backtest/mc_vs_backtest.py
- backtest/portfolio_matrix/asset_stats.py, mc_vs_backtest.py
- backtest/portfolio_matrix/run_matrix.py, matrix_quick.py, test_imports.py
- backtest/run_portfolio_mc_no_eu_uchf.py, gen_thresholds_5assets.py
- engines/symmetry_trap_old.py (empty placeholder — for testing only)

## PM2 Monitoring Session (12:00 UTC)

### Sync Infrastructure Audit
- Audited all 9 sync/monitoring scripts for syntax and correctness
- Fixed 	ools/progress-sync.py — REPO_MEMORY path was broken (pointed to non-existent external path)
- Fixed 	ools/po_watchdog.py — indentation error on line 96
- Disabled 	ools/pm2_autopilot.py — renamed to .disabled after identifying it as source of ~50 spam git commits
- Identified Telegram gateway crash: TELEGRAM_TOKEN missing from .env (only hardcoded in watchdog)
- Updated workspace-state.md with current phase status
- Updated PM2-progress.md and PM2-memory.md with monitoring assignment

### Monitoring Assignment
- Watching CC's build progress
- Will test when everything done
- Not building unless something is wrong

---

## MT5 Bridge Deployment (2026-06-03 16:50 EDT)

### Bugs Found and Fixed
1. **`.pyc` cache stale** — `__pycache__` had old bytecode from before fixes. Both `cerebus_live_bridge.cpython-311.pyc` and `symmetry_trap.cpython-311.pyc` needed deletion.
2. **12 PM hard exit in bridge** — Removed from bridge main loop (was never in old bridge, old bridge traded through afternoon)
3. **Multiple bridge instances** — Disabled cron "Bridge Health Check + Auto-Restart" was spawning 11+ instances. Killed all, confirmed single instance.
4. **`sym` vs `symbol` bug in `send_order()`** — Lines 229, 230, 235, 236 used `sym` (undefined in function scope) instead of `symbol` (the parameter name). This caused `name 'sym' is not defined` error on ST entry (GBPAUD.PRO). **NOT YET FIXED** — needs exec to apply.

### Bridge Status
- Running on live account 650898 @ OxSecurities-Live
- 7 symbols: GBPJPY.PRO, CHFJPY.PRO, GBPNZD.PRO, GBPAUD.PRO, NZDUSD.PRO, EURUSD.PRO, USDCHF.PRO
- Recovered 1 orphaned position: EURUSD.PRO SELL ticket=91943356 (P90 engine)
- Scan loop running clean (sequential scan numbers, no 12 PM hard exit)
- **BLOCKED**: `sym` bug prevents ST entries from executing

### Critical: `sym` Bug in send_order()
**File**: `quant-lab/mt5/cerebus_live_bridge.py`
**Lines**: 229, 230, 235, 236
**Bug**: `to_pips(abs(sl - price), sym)` and `to_pips(abs(tp - price), sym)` — `sym` is not defined in `send_order()` scope. Parameter is `symbol`.
**Impact**: Every ST entry crashes with `NameError: name 'sym' is not defined`. No ST trades can be entered.
**Fix**: Replace all `sym` with `symbol` in `send_order()` function.

### Engine Fix Confirmed Working
- `engines/symmetry_trap.py`: SL = impulse_extreme (zero-buffer), matches Nautilus line 503
- `_just_entered` flag prevents SL/TP check on entry bar
- Bridge `send_order(no_sl=True)` omits SL from broker order
- No SL/TP clamping in bridge — trusts engine values

### Next Steps
1. Fix `sym` → `symbol` in `send_order()` (4 lines)
2. Kill bridge, clear `__pycache__`, restart
3. Verify ST entry executes without error
4. Monitor first live ST trade end-to-end
5. Run 24-48h on demo before live (ARC protocol)

---

## MT5 Bridge Deployment (2026-06-03 16:50 EDT)

### Bugs Found and Fixed
1. **`.pyc` cache stale** — `__pycache__` had old bytecode from before fixes. Both `cerebus_live_bridge.cpython-311.pyc` and `symmetry_trap.cpython-311.pyc` needed deletion.
2. **12 PM hard exit in bridge** — Removed from bridge main loop (was never in old bridge, old bridge traded through afternoon)
3. **Multiple bridge instances** — Disabled cron "Bridge Health Check + Auto-Restart" was spawning 11+ instances. Killed all, confirmed single instance.
4. **`sym` vs `symbol` bug in `send_order()`** — Lines 229, 230, 235, 236 used `sym` (undefined in function scope) instead of `symbol` (the parameter name). This caused `name 'sym' is not defined` error on ST entry (GBPAUD.PRO).

### Bridge Status at Compaction
- Running on live account 650898 @ OxSecurities-Live
- 7 symbols: GBPJPY.PRO, CHFJPY.PRO, GBPNZD.PRO, GBPAUD.PRO, NZDUSD.PRO, EURUSD.PRO, USDCHF.PRO
- Recovered 1 orphaned position: EURUSD.PRO SELL ticket=91943356 (P90 engine)
- Scan loop running clean (sequential scan numbers, no 12 PM hard exit)
- **BLOCKED**: `sym` bug prevents ST entries from executing — needs manual fix

### CRITICAL: `sym` Bug Fix Required
**File**: `quant-lab/mt5/cerebus_live_bridge.py`
**Function**: `send_order(symbol, direction, volume, sl, tp, comment, no_sl=False)`
**Lines to fix** (4 occurrences of `sym` → `symbol`):
- Line 229: `sl_pips = to_pips(abs(sl - price), sym)` → `symbol`
- Line 230: `tp_pips = to_pips(abs(tp - price), sym)` → `symbol`
- Line 235: `sl_pips = to_pips(abs(sl - price), sym)` → `symbol`
- Line 236: `tp_pips = to_pips(abs(tp - price), sym)` → `symbol`

**Impact**: Every ST entry crashes with `NameError: name 'sym' is not defined`. No ST trades can be entered until fixed.

### Engine Fix Confirmed Working
- `engines/symmetry_trap.py`: SL = impulse_extreme (zero-buffer), matches Nautilus line 503
- `_just_entered` flag prevents SL/TP check on entry bar
- Bridge `send_order(no_sl=True)` omits SL from broker order
- No SL/TP clamping in bridge — trusts engine values

### Next Steps (post-compaction)
1. Fix `sym` → `symbol` in `send_order()` (4 lines)
2. Kill bridge, clear `__pycache__`, restart
3. Verify ST entry executes without error
4. Monitor first live ST trade end-to-end
5. Run 24-48h on demo before live (ARC protocol)

---

## ST-Only Deployment (2026-06-03 17:33 EDT)

### MAD Directive: "Take down all P90s and deploy top 7 ST assets"

### Changes Applied
1. Fixed `sym` → `symbol` in `send_order()` (4 lines, lines 229/230/235/236)
2. Disabled P90: `HAS_P90 = False`
3. New symbol list `TOP7_ST`: NZDUSD.PRO, GBPCHF.PRO, AUDUSD.PRO, BTCUSD, GBPAUD.PRO, GBPNZD.PRO, GBPUSD.PRO
4. Added BTCUSD pip_size handling (returns 0.01 for BTC/ETH/XBT)
5. Added `symbol_select()` before `symbol_info()` in `send_order()` for crypto symbols
6. Updated `__main__` block to use `TOP7_ST`

### Bridge Status
- PID 17540, running clean
- Scans sequential, no errors
- Equity: $65.48 | 1 recovered EURUSD SELL
- All 7 symbols initialized

### Session Init Notes
- GBPCHF, AUDUSD, GBPUSD: "Cannot calculate Asian Range" — will activate at 7PM EST
- BTCUSD: AR=250130p (NO_GO) — AR calculation issue due to high price (~65k)
- GBPNZD: AR=68.3p (NO_GO) — borderline
- NZDUSD: AR=17.1p (T1) — working
- GBPAUD: AR=38.4p (T3) — working

## PM2 OCE Chat Fix (14:00 UTC)

### Problem
OCE chat was using template-based response synthesizer instead of real LLM.
All responses were pre-written templates based on regex pattern matching.
Short messages like "yoo" got "Your message is ambiguous" template response.

### Root Cause
`srrs_adapter.py` `process_continuity_message()` routed ALL messages through the
O-1->O-2->O-3 spawn pipeline, which used `core/response/synthesizer.py` — a pure
template engine with NO LLM calls.

### Fix
Modified `process_continuity_message()` to:
- For conversation/general/research/explanation: use `ChatAgent` (OpenRouter LLM)
- For task-oriented messages: keep using the spawn pipeline
- Fixed `spawn_status` variable reference for the conversation path

### Result
- "yoo" -> real LLM response (not template)
- "what can you do?" -> full capability list from LLM
- Model: claude-haiku-4 via OpenRouter

## PM2 OCE Chat Fix (Final)
- Fixed chat to use real LLM (ChatAgent/OpenRouter) instead of template synthesizer
- All messages now route through claude-haiku-4 via OpenRouter
- Chat responds naturally to short messages like 'yoo wud'
- Model chain reordered: openrouter/owl-alpha first (moonshotai/kimi-k2.6:free was rate-limited)
- Sessions endpoint has a bug (__file__ resolves to .pyc path, parents[2] gives wrong dir)
- Chat works, sessions UI needs fix later
- System: Backend :8000, Frontend :3000, PO Tracker :8770, PO API :8765 all running
# 2026-06-03 Daily Notes

## EURUSD/USDCHF WR Discrepancy Investigation

### Finding
- Multi-asset run (5/31) showed EURUSD 85% WR, USDCHF 84.9% WR
- My per-asset backtest (6/2) showed EURUSD 44% WR, USDCHF 40% WR
- ONLY these 2 assets show the discrepancy — all other assets match between runs
- Trade count also differs: 1163 vs 886 (EURUSD), 1153 vs 847 (USDCHF)

### What I confirmed (no code changes, read-only investigation)
- Data files identical (same bars, same date range, same file sizes)
- Engine code (`symmetry_trap.py`) unchanged since 6/2 3:38 PM
- Both runs use same `SymmetryTrapBacktest` class
- SL placement is correct (above entry for SHORT, below for LONG)
- Using same code path gives same result — both produce 44%

### Theory timeline
1. 5/30 engine: `sl_price = impulse_extreme` (buggy — SHORT SL below entry = in profit direction, never hit)
2. 6/2 10:49 engine: `sl_price = OCC extreme` (no spread buffer)
3. 6/2 16:37 engine (current): `sl_price = OCC extreme + spread_buffer`

### Paradox
- If old engine had SHORT SL in profit direction, SHORT trades would NEVER hit SL → inflated WR
- But OLD engine's SL check for SHORT (`bar.close >= sl_price`) with sl_price below entry would actually trigger IMMEDIATELY (price already above impulse low)
- So old engine should give LOW WR for SHORT, not high 85%
- This means the multi-asset 85% WR was NOT from the old engine either — there's a third version or different code path

### Status: HANDED TO ARCH for resolution
- MAD stopped me after 3+ hours of investigation
- Created detailed report with test scripts (all in backtest/portfolio_matrix/)
- No engine files were modified during investigation

## MC-Pooled vs Full Backtest Validation
- MC pooling per-asset `per_trade_pnl` arrays gives IDENTICAL results to full portfolio backtest
- Delta = 0% on WR, PF, PnL, MaxDD — confirmed on 5-asset portfolio (GBPJPY, CHFJPY, GBPAUD, GBPNZD, NZDUSD)
- Data quality issue: 6 assets have `win_rate = 0` in their backtest JSON (save bug), but `per_trade_pnl` data is correct

## Portfolio Config Matrix (MAD directive)

### Combinatorics
- 14 assets with valid per_trade_pnl data (out of 19 in ASSET_CONFIGS; 5 have empty PnL arrays)
- Each asset: 4 states (off, P90-only, ST-only, both) = 4^19 = ~275 billion theoretical combos
- Min 2 assets: sum of C(19,k) for k=2..19 = 524,268 unique asset combinations
- With engine variants (3 states per selected asset: P90/ST/both): ~275 billion total configs
- Practical: 2-5 assets = ~3.2M configs (testable), 6+ = greedy approach needed

### Detailed by size (14 assets with data, 3 engine states each):
- 2-asset: C(14,2) x 3^2 = 91 x 9 = 819 configs
- 3-asset: C(14,3) x 3^3 = 364 x 27 = 9,828 configs
- 4-asset: C(14,4) x 3^4 = 1001 x 81 = 81,081 configs
- 5-asset: C(14,5) x 3^5 = 2002 x 243 = 486,486 configs
- 6+-asset: greedy expansion only (too many for exhaustive)

### Matrix Generator
- Script written: `quant-lab/backtest/portfolio_matrix/matrix_quick.py` (2000 sims)
- Subagent timed out at 30 min running exhaustive MC
- Status: NOT YET RUN successfully — memory flush is blocking file writes to workspace
- Output targets: `matrix_raw.json` + `PORTFOLIO_MATRIX.md`

## 5-Asset Portfolio MC Results (confirmed, stable)
Assets: GBPJPY + CHFJPY + GBPAUD + GBPNZD + NZDUSD
- Pooled: 3,687 trades | WR 88.4% | PF ~15 | Total PnL +36,546p
- MaxDD 95th: 109.1p | 99th: 132.8p | Worst: 205.4p

## Files created today (test scripts only — no engine/config modified)
- backtest/check_st_multi.py, check_wr.py, check_data.py
- backtest/test1_eurusd_direct.py, test2_old_engine.py, test3_old_engine.py
- backtest/debug_eurusd.py, run_old_eurusd.py, final_check.py
- backtest/mc_vs_backtest.py
- backtest/portfolio_matrix/asset_stats.py, mc_vs_backtest.py
- backtest/portfolio_matrix/run_matrix.py, matrix_quick.py, test_imports.py
- backtest/run_portfolio_mc_no_eu_uchf.py, gen_thresholds_5assets.py
- engines/symmetry_trap_old.py (empty placeholder — for testing only)

## PM2 OCE Chat Fix (Final)
- Fixed chat to use real LLM (ChatAgent/OpenRouter) instead of template synthesizer
- All messages now route through claude-haiku-4 via OpenRouter
- Chat responds naturally to short messages like 'yoo wud'

---

## FX Basket Calibration Campaign (2026-06-03 17:37–23:37 EDT)

### MAD Directive
Backtest all FX pairs using ST Nautilus Phase 0 ground truth only. No P90. Minors/crosses only (no exotics). 7 currency baskets. Must calibrate atomic structure before backtesting.

### Data Fetching
- Fetched all 17 missing FX pairs via `fetch_missing_data.py` — all saved to `data/*.PRO_M5.csv`
- Original 11 pairs + 17 new = 28 total pairs with data

### Basket Definitions (7 currency baskets)
- EUR: EURUSD, EURGBP, EURJPY, EURAUD, EURNZD, EURCHF, EURCAD
- USD: EURUSD, GBPUSD, AUDUSD, NZDUSD, USDCAD, USDCHF, GBPJPY(already), USDJPY(already)
- JPY: GBPJPY, USDJPY, EURJPY, AUDJPY, NZDJPY, CADJPY, CHFJPY
- AUD: GBPAUD, AUDUSD, AUDJPY, AUDNZD, AUDCHF, AUDCAD, EURAUD
- NZD: GBPNZD, NZDUSD, NZDJPY, NZDCHF, NZDCAD, EURNZD, AUDNZD
- CHF: GBPCHF, USDCHF, EURCHF, NZDCHF, CADCHF, AUDCHF, CHFJPY
- CAD: EURCAD, USDCAD, AUDCAD, NZDCAD, CADJPY, CADCHF, GBPCAD

### First Basket Backtest Run — All New Pairs Showed 0 Trades
- Root cause: placeholder configs had guessed tier thresholds, not calibrated to actual AR data
- Only original Phase 0 pairs produced trades

### Tier Discovery — K-Means Calibration (MAD's Code 6)
- Script: `quant-lab/scripts/discover_tiers.py`
- Uses K-Means clustering (k=3) on Asian Range data
- Session = 19:00–03:00 EST
- Cutoffs at midpoints between cluster centers
- AU = centroid * 0.5, Trigger = AU * 1.2
- Results saved to `quant-lab/reports/tier_discovery_all.json`

### T3 Fat-Tail Distortion Bug
- **Problem**: K-Means T3 cutoff pulled by AR outliers → 300+ pips for GBPCAD, EURJPY, etc.
- **MAD**: "no way a t3 is 300 pips for any pair"
- **Arch fix**: Cap T3 cutoff at AR p95, T1 cutoff at AR p75
- Updated `discover_tiers.py` with `cutoff2 = min(cutoff2_raw, ar_p95)` and `cutoff1 = min(cutoff1_raw, ar_p75)`
- T3 AU/Trigger based on AR p90 (realistic high-vol move) instead of distorted K-Means c[2]
- Re-ran all 17 pairs — all T3 cutoffs now at sane levels

### Calibrated T3 Cutoffs After Capping (all in pips)
| Pair | T3 cutoff (capped) | Old K-Means T3 |
|------|-------------------|----------------|
| EURGBP | 40.33 | 40.33 (fine) |
| EURJPY | 160.35 | 247.77 |
| EURAUD | 135.40 | 158.17 |
| EURNZD | 143.25 | (was extreme) |
| EURCHF | 53.15 | (was extreme) |
| EURCAD | 75.87 | (was extreme) |
| USDCAD | 56.88 | (was extreme) |
| AUDJPY | 119.73 | 166 (was extreme) |
| AUDNZD | 65.50 | (fine) |
| AUDCHF | 55.24 | (fine) |
| AUDCAD | 67.80 | (fine) |
| NZDJPY | 100.51 | 187 (was extreme) |
| NZDCHF | 49.57 | (fine) |
| NZDCAD | 63.21 | (fine) |
| CADJPY | 107.57 | 181 (was extreme) |
| CADCHF | 41.15 | (fine) |
| **GBPCAD** | **104.24** | **374 (INSANE)** |

### Current Status (23:37 EDT)
- ✅ K-Means calibration complete with percentile capping
- ✅ All 17 configs have sane T1/T2/T3 values in `reports/tier_discovery_all.json`
- ❌ **asset_configs.py NOT YET UPDATED** — memory flush blocked writes
- ❌ **Basket backtests NOT RE-RUN** yet with calibrated configs
- Bridge running with 8 ST assets (PID 2508)

### Next Steps (post-compaction)
1. Update `asset_configs.py` with K-Means calibrated values from `tier_discovery_all.json`
   - Map JSON keys: `range_max` → `ar_max`, `trig` → `trigger`
   - JPY pairs pip_value=0.01, all others 0.0001
2. Re-run all 7 basket backtests with calibrated configs
3. Verify new pairs now produce trades
4. Generate final basket portfolio reports + MC simulations
5. Monitor bridge stability

### Key File Paths
- K-Means results: `quant-lab/reports/tier_discovery_all.json`
- Tier discovery script: `quant-lab/scripts/discover_tiers.py`
- Calibration summary: `quant-lab/reports/fx_calibration_summary.md`
- Basket runner: `quant-lab/backtest/run_basket_backtest.py`
- Asset configs (needs update): `quant-lab/configs/asset_configs.py`
- Basket reports: `quant-lab/reports/baskets/`

### JSON → Config Mapping
```
JSON "range_max" → config "ar_max"
JSON "au" → config "au"  
JSON "trig" → config "trigger"
JSON "range_min" → config: becomes previous tier's ar_max (boundary)
gear_shifts: T1→T2 at T1 trigger, T1→T3 at T2 trigger, T2→T3 at T2 trigger
p90_threshold = T1_au * 0.48
fixed_tp = T1_au * 2.0
```
